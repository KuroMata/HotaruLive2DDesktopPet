# Live2D Companion 运行与排错说明

透明置顶的 Live2D 桌宠（默认 Hotaru2024 模型）+ 内置聊天面板，聊天通过本机 WorkBuddy 的 ACP 本地协议桥接，实现"跑 Live2D 的同时正常和 WorkBuddy 对话"。

---

## 一、前置条件

项目依赖已安装完成，且 `D:\live2d-companion\node_modules\electron\dist\electron.exe` 存在（当前版本 Electron 30.5.1，已实测可执行）。

若日后需要重装，在本机执行（务必带上环境变量，否则会踩 C 盘写满与 GitHub 超时两个坑）：

```powershell
$env:ELECTRON_MIRROR  = "https://registry.npmmirror.com/-/binary/electron/"
$env:ELECTRON_CACHE   = "D:\live2d-companion\.electron-cache"
$env:npm_config_cache = "D:\npm-cache"
$env:TEMP = "D:\tmp"; $env:TMP = "D:\tmp"   # 解压临时文件必须指向非 C 盘，否则仍会写满 C:\Temp
Set-Location D:\live2d-companion
& "C:\Users\Administrator\.workbuddy\binaries\node\versions\22.22.2-3\npm.cmd" install --no-audit --no-fund
```

> 注 1：Electron 二进制从国内镜像 `npmmirror` 拉取（GitHub Releases 被墙会超时）。npm 缓存已永久设为 `D:\npm-cache`。
> 注 2：若 `npm install` 长时间无进展且 `.electron-cache` 目录始终未创建，说明 electron 的 `node install.js` 又去撞 GitHub 了。此时直接手动兜底最快：
> ```powershell
> curl.exe -L -o "D:\live2d-companion\.electron-cache\electron-v30.5.1-win32-x64.zip" `
>   "https://registry.npmmirror.com/-/binary/electron/30.5.1/electron-v30.5.1-win32-x64.zip"
> Expand-Archive "D:\live2d-companion\.electron-cache\electron-v30.5.1-win32-x64.zip" `
>   "D:\live2d-companion\node_modules\electron\dist" -Force
> "electron.exe" | Out-File "D:\live2d-companion\node_modules\electron\path.txt" -Encoding ascii -NoNewline
> ```
> 最后一行不可省：`path.txt` 是 electron 包用来定位二进制的文件，postinstall 被中断时它不会被写入，缺失会让 `npm start` 直接抛 `Electron failed to install correctly`。**内容只写 `electron.exe`**（包内部会自行拼上 `dist\`，写成 `dist/electron.exe` 会变成 `dist\dist\electron.exe`）。

---

## 二、如何启动

> **PowerShell 语法提醒（本次踩坑点）**：`cd /d` 是 CMD 的写法，PowerShell 不认，会报 `Set-Location: 找不到接受实际参数"/d"的位置形式参数`。PowerShell 里直接 `Set-Location D:\live2d-companion` 或 `cd D:\live2d-companion` 即可。
> 另一处：以引号开头的路径在 PowerShell 里**不能直接当命令执行**，会报 `表达式或语句中包含意外的标记"start"`（UnexpectedToken）。必须在前面加调用运算符 `&`。

### 方式 A（最省事）：双击 `黑叶萤桌宠.exe`

直接双击 `D:\live2d-companion\黑叶萤桌宠.exe`。它是 PyInstaller 打包的启动器（源码 `_launcher_build\launcher.py`，配置 `_launcher_build\黑叶萤桌宠.spec`，约 8 MB），行为与 `start.cmd` 完全等价：定位项目根 → 清掉 `ELECTRON_RUN_AS_NODE` → 用 `node_modules\electron\dist\electron.exe` 以 `D:\live2d-companion\.` 为参数拉起 Electron（`SW_HIDE`，**命令行窗口连闪都不会闪**）。

> **关键性质：它跑的是当前源码，不是打包产物。** 启动器自身不含 app 代码，每次启动都从项目根加载 `main.js` 与 `app\`。所以**改完源码直接双击这个 exe 就是最新版，不需要重新打包**。（`dist\` 里的安装包与 `win-unpacked` 才是静态快照，见「十、文件结构」。）
>
> 启动器自带项目根定位（`_proj_root()`）：按「exe 所在目录 → 当前工作目录 → `argv[0]` 目录」的顺序，找同时含 `main.js` 与 `node_modules\electron\dist\electron.exe` 的目录。因此把它复制到别处也能用，只要目标目录里项目是完整的。
>
> 出错时它弹 Windows 消息框显示 traceback，并把记录追加写入 `launcher_debug.log`（每次 spawn 的时间与 pid）。想确认"上一次双击是什么时候、拉起了哪次"看这个文件。

### 方式 A2：双击 `start.cmd`

与上面等价，区别是用 `PowerShell Start-Process` 拉起：会先切到自身所在目录、检查二进制、强制清掉 `ELECTRON_RUN_AS_NODE`，再脱离控制台启动——**会极短地闪一个控制台窗口后自动关闭**，属正常现象。

> 启动器内部传给 electron 的是 `%~dp0.`（结尾带点）。`%~dp0` 结尾自带反斜杠，若写成 `%~dp0"` 会形成 `\"`，被 Electron 当成转义引号，弹出 `Cannot find module 'D:\live2d-companion"'`。这个坑已修，改造启动命令时请保留那个末尾的点。

> **维护须知（重要）**：`start.cmd` 与 `start-debug.cmd` 必须保持**纯 ASCII 内容 + CRLF 换行**，里面绝对不要出现中文，哪怕是 `rem` 注释或 `echo` 提示。原因见「排错 → 0」表格最后一行：cmd.exe 用 GBK 代码页按字节解析 `.cmd`，中文的 UTF-8 字节序列会把行尾 `\r` 吃掉，导致整段脚本被合并、截断而失效。中文说明统一写在本文件里。

### 方式 A-Debug：双击没反应时用它

如果双击 `start.cmd` 后命令行一闪而过、桌宠却没出现，**请改用 `D:\live2d-companion\start-debug.cmd` 双击启动**。它的区别是 electron 在前台运行、命令行窗口**不自动关闭**——任何启动错误（环境变量没清、端口 18765 被占、模型加载失败等）都会直接显示在窗口里。把窗口里的红色报错截图或文字发回，即可精准定位。

> 另外，每次启动的关键步骤都会写入 `D:\live2d-companion\app.log`（启动时间、electron 版本、是否退化成 Node、静态服务是否起来、窗口与托盘是否创建）。双击没反应时，也可直接把这个文件的内容发回。

### 方式 B：PowerShell

```powershell
Set-Location D:\live2d-companion
& "C:\Users\Administrator\.workbuddy\binaries\node\versions\22.22.2-3\npm.cmd" start
```

注意行首的 `&`（调用运算符）不能省略。若嫌路径长，也可以先 `$env:PATH = "C:\Users\Administrator\.workbuddy\binaries\node\versions\22.22.2-3;$env:PATH"`，之后直接 `npm start`。

### 方式 C：绕过 npm，直接跑 electron

```powershell
Set-Location D:\live2d-companion
& "D:\live2d-companion\node_modules\electron\dist\electron.exe" .
```

`&` 同样不可省；`.` 表示以当前目录为应用根，所以必须先切到项目目录。

### 方式 D：CMD（命令提示符）

CMD 下 `cd /d` 与引号写法都合法：

```cmd
cd /d D:\live2d-companion
"C:\Users\Administrator\.workbuddy\binaries\node\versions\22.22.2-3\npm.cmd" start
```

启动后应弹出一个**透明、无边框、置顶**的小窗口：顶部标题栏（Live2D · WorkBuddy + 聊天/锁定/重置/设置），中间是 Hotaru2024 模型，**底部输入框常驻**（聊天记录可用「聊天」按钮展开/收起），系统托盘出现一个粉色圆形图标。

> 「设置」按钮会打开一个独立的设置窗口（9 个分页）。**隐藏窗口**改由托盘菜单承担（底栏原来那个「隐藏」按钮已换成「设置」）。

---

## 三、窗口与交互

### 托盘（替代任务栏）
- 程序启动后**不出现在任务栏**（窗口设了 `skipTaskbar`），而是在**系统托盘**放一个图标。
- **左键托盘图标**：显示 / 隐藏窗口。
- **右键托盘图标**：弹出菜单 —— 显示/隐藏窗口、**锁定/解锁模型位置与缩放**、**重置模型位置与缩放**、**退出**。
- 双击 `start.cmd` 后，那个命令行窗口会**自动关闭**，但桌宠随托盘常驻；之后一切通过托盘图标操作。
- 注意：只有托盘菜单里的「退出」才会真正关闭程序；标题栏的「隐藏」只是把窗口藏起来（托盘里还能再调出来）。

### 底部控制栏按钮（从左到右：聊天 / 透明·毛玻璃 / 锁 / 重置 / 隐藏）
> 顶部的标题栏已去掉（原来那条灰条会被系统标题栏压住、也不好看）。现在**所有按钮都在窗口最底端的一排**，左侧是 `LIVE2D · WORKBUDDY` 霓虹字（窗口变窄时它会自动省略，按钮始终完整可见）。

- **聊天**：展开/收起聊天**记录**。输入框始终常驻（见下），不受此按钮影响。
- **透明 / 毛玻璃**：切换窗口背景模式，二态循环，选择会被记住（详见下一节）。
- **锁**：锁定 / 解锁模型的位置与缩放。锁定后拖动和滚轮失效，避免误碰。锁定状态会被记住，下次启动仍是锁定。
- **重置**：一键把模型的缩放和位置恢复到默认（居中适配），并解除锁定。**模型被拖到看不见时，用这个按钮把它找回来。**
- **隐藏**：把窗口隐藏到托盘。

### 背景模式（透明底 / 毛玻璃底）
- **透明底（默认）**：窗体除霓虹灯带、聊天面板、底部控制栏外**完全透明**，模型直接浮在桌面上。
- **毛玻璃底**：给窗体套一层半透明磨砂贴底（`rgba(10,14,22,0.34)` + 一点顶部光泽），可以透出下方的窗口内容。
- 切换按钮在底栏第二位，标签显示**当前模式**（显示「透明」表示现在是透明底）。
- 选择存在 `localStorage` 的 `l2d-bg-glass-v1`；想改默认值就改 `config.json` 的 `startupBackground`（`"clear"` / `"glass"`），仅在从未点过按钮时生效。
- **聊天面板与底部控制栏始终是毛玻璃**（`backdrop-filter: blur(16px)`），它模糊的是身后的模型 —— 这一层是实打实生效的。
- 已知限制：窗体这一层加了 `backdrop-filter` 也**不会**把桌面的内容模糊掉。透明窗口背后的桌面不属于页面的 backdrop，Chromium 根本没东西可采样（要让桌面真正被模糊需要系统级 acrylic/mica，本项目未启用）。所以毛玻璃模式表现为"半透明+提亮"，而不是"桌面被糊化"。

### 模型操作（在模型画面上）
- **移动**：在模型上按住左键拖动。**可移动范围很大** —— 允许把模型拖到几乎完全移出窗口（只留一小块压边，避免彻底丢掉），所以能摆到任意想要的位置。
- **缩放**：在模型上滚动鼠标滚轮，以光标为锚点放大/缩小（范围约默认的 20%~3000%）。
- **记忆**：缩放大小与位置（以及锁定状态）会自动保存，下次启动直接恢复。
- **兜底**：位置/缩放记在了 `localStorage` 的 `l2d-layout-v1`。如果某次摆得太偏、重启后找不着模型，点底栏「重置」即可回到居中适配（重置是幂等的：无论当前缩放到多少，结果都一样）。
- 这些操作只在**未锁定**时有效；锁定后光标变为普通箭头，拖动与滚轮均不响应。

### 输入框常驻
- 聊天输入框**始终显示在模型底端**，不会随「聊天」按钮折叠而消失；「聊天」按钮只控制上方聊天记录的展开/收起。
- 默认占一行，输入变长会向下自动撑高（约 6 行后转为内部滚动），发送后回到一行。
- 回车发送，Shift+Enter 换行。

### 聊天记录里的系统提示
- **首次打开**（该机器上第一次）会写入一条问候：`▎神经链路已同步 / 渲染核心在线，口型与呼吸同步模块已加载。/ 待机中，请下达指令。`
- 之后每次启动只写一行状态：`渲染核心已挂载 · 正在建立链路…`；链路是重试若干次后才通的，会补一条 `链路已建立，等待指令。`
- 这些是居中的等宽小字样式（`.msg.system`），与对话气泡区分开。

### 窗口
- **移动整个窗口**：拖动**底部控制栏**的空白处（品牌字与按钮之间；底栏整体是拖拽区，按钮本身不拖）。
- **改窗口大小**：拖动窗口边缘，模型会自动重新适配（已手动调过位置/缩放时则保留你的布局）。
- **置顶**：默认始终置顶（`alwaysOnTop: screen-saver`）。如需取消置顶需在 `main.js` 调整，当前未挂 UI 按钮。
- **窗口内 28px 的透明内边距（`--gap`）是刻意留的**：外发光会被窗口矩形**硬切**，切口深度等于光晕在窗口边缘处的强度。把 body 底色设成平坦深灰（模拟桌面）后逐像素量"最边缘一行比桌面亮多少"，实测：`gap=16px → +3.8`（肉眼可见一条直边）、`20px → +1.5`、`24px → +0.4`、`28px → 0.0`、`32px → 0.0`。取 **28px**，同时把 `config.json` 的窗口宽高 +24px（`384 × 564`），保证窗体本身仍是 `328 × 508`、模型可用区域不变。
  - 由此推出一条约束：**想加大外发光的模糊半径，必须同步加大 `--gap`**，否则刀口会回来。当前最大模糊 24px（呼吸高相位），`28 - 24 = 4px` 是余量。
  - 注意 `--gap` 是绝对像素、不随窗口缩放，所以你把窗口拉大拉小都不影响这个结论。
- **玻璃面板与灯带框内沿对齐**（`#chat`/`#bottom-bar` 的 `margin` 为 0，底栏圆角取 `--radius`）。早先它们浮在框内 9px，代价是面板左右各有一条竖直硬边、与灯带的直边平行并列，观感上就是两道直线；对齐后玻璃与灯带成为同一扇窗，圆角与灯带同心。

### 关于"边缘硬边"这件事（改样式前请先读）
这条反馈修过三轮，最终定位到**四个**独立来源，改 `styles.css` 时不要把它们再引进回来：

1. **灯带辉光"外强内空"**。只写外侧 `box-shadow` 时，管子外侧紧邻处 α≈93，内侧紧邻处只有 α≈24 —— 观感上就是一条硬线而不是灯管。现在外侧 3 层 + 内侧 4 层（5/14/30/64px），两侧衰减对称（内侧 `57,48,38,34,28,25,23,21,18,18`）。自检会断言内侧层数 ≥ 4。
2. **等宽"弱环"鞘层**。曾用 `::after` 加过一圈 padding 5px / α0.20 的环做衬底，结果它自身的内边界就留下 16% 的 α 台阶（89→49）；改成 9px / α0.09 仍有 9%（47→23）。**等宽环的内外边界天然是硬边**，已移除，内晕改由多层 `box-shadow` 承担（模糊天然连续）。自检会断言 `#window-frame::after` 的 `content` 为 `none`。
3. **玻璃面板浮在框内**（见上一条）。
4. **外发光的"刀口"**。光晕在窗口矩形处被硬切 —— 这是"边缘像被切了一刀"的真正原因。只能靠**留够空间**根治：`--gap` 取 28px（实测 16px 时切口 +3.8、28px 归零）。**不要**试图用 mask 或滤镜去柔化窗口边缘 —— 窗口矩形是系统层裁切，CSS 够不到。
5. **模型被画布矩形切出直边**。模型放大或拖到边缘时，超出画布的部分会被裁。`#stage`（上下）与 `#live2d`（左右）各挂一个 24px 的渐隐 mask，用 6 段 stops 近似 smoothstep（单段线性在靠近边缘时导数偏大，看上去仍像"被裁"）。这两条渐变定义在 `:root` 的 `--fade-v` / `--fade-h`。

---

## 四、切换模型（设置里点一下，免重启）

**推荐做法：托盘「设置…」→「模型」分页。**

1. **模型库目录**填模型库**根目录**（可以直接指向 VTube Studio 的 `Live2DModels` 目录）。留空 = 用程序内的 `app\models`。
   > **不要填到某个具体模型文件夹**（例如 `...\Live2DModels\nyata`）：那样下拉里只会出现那一个模型。填根目录才会一次列出全部（本机实测 38 个）。
2. 程序自动递归扫描该目录下所有 `*.model3.json`，**加密的 `*.model3.json.enc` 也能扫到**，下拉里按「名字 · 相对路径」列出。
3. 选中后点**「载入模型」** → 立即热切换。**不用重启程序，聊天不中断。**

### 切换后，「台词」与「表情与动作」两个分页会自动跟着换

点「载入模型」时，设置窗里另两个以"当前模型"为基准的分页会**立即同步到新模型**，不需要关掉重开设置窗：

- **「台词」分页**：模型名换成新模型，台词档按新模型重查绑定表；没绑过档就自动回退到通用档 `default`，并在那一行写明"该模型未绑定台词档，已回退到通用档 default"。要给新模型一个专属档，就在这一页选好档后点「绑定到此模型」。
- **「表情与动作」分页**：重新扫描**新模型目录**，列出它自己的 `*.motion3.json` / `*.exp3.json` 候选；上一个模型的候选与"已加载"标记一并作废重算。等桌宠把模型真正载入完，它会回报该模型实际加载到的数量（例如「引擎已加载：动作 2 / 表情 12」）。
- **触发绑定**：表情/动作的目标候选、以及"哪些台词"多选，都随新模型和它对应的台词档一起换。

> 换模型的场景**不需要**点「保存并重新载入模型」——切模型本身就会让桌宠热重载，并按新模型的注入配置生效。那个按钮是留给"改了素材关联、但不想换模型"的场景的。

**换模型库目录也不需要重启**：服务层每次请求才去读当前库根目录（`modelServeBase()`），所以改完点「重新扫描」即可看到新目录里的模型。

### 换模型后语音怎么办

**不会沿用原模型的语音。** 新模型按「台词」分页里为它绑定的台词档走；没绑过就落到通用档 `default`（只有通用话术，全部走 TTS 现场合成）。这样避免"换了模型却还在用上一个角色的声音"。

### 也可以直接改 config.json

不想开设置窗时，改 `D:\live2d-companion\app\config.json`：

```json
{
  "modelServeBase": "",
  "modelUrl": "/models/Hotaru2024/hotaru2024.model3.json",
  "cubismCore": "cubism3"
}
```

- `modelServeBase`：模型库根目录。**留空字符串表示用程序内的 `app\models`**（当前默认就是留空，模型实际放在 `app\models\Hotaru2024\`）。
- `modelUrl`：`/models/<模型文件夹>/<xxx.model3.json>`，`/models/` 前缀固定，后面是相对 `modelServeBase` 的路径。文件夹名大小写敏感。
- `cubismCore`：仅作记录，运行时由 `app/vendor/live2dcubismcore.min.js` 决定，**不用改**。
- 模型格式要求：必须是 `.moc3`（Cubism 3/4/5），当前 vendor 的 Core 5.1.0 都能吃。老的 `.moc`（Cubism 2）不支持。
- 若换的模型贴图很大（如 8192），内存占用会进一步上升；换完可跑一次 `tools\selfcheck\` 确认加载成功。
- 改 `modelUrl` 需**重启程序**（设置窗里的「载入模型」才是热切换）。

---

## 五、台词 · 三语言 · 语音来源

分页位置：设置 →「台词」。台词内容存在 `app\data\lines\profiles\<档 id>.json`，**改动即时保存**，不会动到 program code。

### 台词档与语言

- **台词档**：一整套台词（**点击台词池** + **待机台词池**）。每个模型可以绑定不同的档（存在 `config.lineProfile` 里，形如 `{"Hotaru2024/hotaru2024.model3.json": "hotaru"}`）。没绑定的模型落到 `default` 通用档。
- **显示语言 / 语音语言**：分别决定气泡里显示哪种文字、朗读哪种语言。**中文 / 日本語 / English 三语都支持**。
- 某条台词若缺对应语言的文本，会**回退到中文文本**（并用中文那份语音来源），不会报错也不会哑掉。
- **默认引擎 / 音色**：给"语音来源 = TTS"的台词用。Edge TTS 的中/日/英音色最全（需联网）；CosyVoice / IndexTTS 是本地克隆音色。

### 每条台词的语音来源（三选一，且**每种语言可以各选各的**）

| 来源 | 说明 |
|---|---|
| **预生成文件** | 项目内 `app/data/lines` 下的 wav。零延迟，就是原来那批离线语音 |
| **自录音频** | 选你自己录的 `wav/mp3/m4a/aac/ogg/opus/flac/weba`。**引用原路径，不复制文件** |
| **TTS 合成** | 运行时现场合成，可单独指定引擎与音色（留空用上面的默认值） |

### ⚠️ 自录音频是"引用原路径"，请别移动源文件

自录音频走同源代理 `/api/user-audio?path=<绝对路径>`。页面来自 `http://127.0.0.1`，直接 `file:///D:/x.wav` 会被 Chromium 拦截，所以必须走这个代理。

**出于安全，该接口只放行"被某条台词档以 `src:"abs"` 引用过"的路径**（白名单在每次请求时重新收集）。实测行为：

| 请求 | 结果 |
|---|---|
| 未被任何台词档引用 | `403 not referenced by any line profile` |
| 扩展名不是音频 | `403 unsupported audio type` |
| 没带 `path` 参数 | `400 missing path` |
| 已被引用（`src:"abs"`） | `200`，`Content-Type: audio/wav`，字节数与源文件完全一致 |

所以：**移动 / 改名 / 删除你录好的音频后，对应台词会播不出来**（需重新在设置里选一次文件）。路径大小写不敏感（Windows 下 `D:\A.wav` 与 `d:\a.wav` 视为同一个）。

### 试听

每条台词都有「试听」，按当前设置（来源 / 引擎 / 音色）立刻朗读该条。台词档与语言区也有整档的默认音色试听与音色下拉（引擎音色列表从 TTS 侧车 `/health` 实时取，不是写死的）。

---

## 六、表情与动作（模型自带的 exp3 / motion3）

分页位置：设置 →「表情与动作」。分两个控件：**素材关联**（把文件挂进模型引用）+ **触发方式与绑定**（决定怎么播）。

### 为什么需要"素材关联"

很多模型目录里明明有 `idle.motion3.json`、`sleep.motion3.json`，却**没写进 `model3.json` 的 `FileReferences.Motions`** —— 引擎根本不会加载它们，于是"选了也播不出来"。

勾选文件、填好组名后，程序会把配置写进 `config.modelAssets`，并在**服务层返回 `model3.json` 时动态补写 `FileReferences.Motions`**（**不改磁盘上的模型文件，也不碰加密的 `.enc`**）。点「保存并重新载入模型」即可生效。

> **⚠️ 组名大小写有讲究**：组名若写成 `Idle`（首字母大写），引擎会把它当成**自动循环的待机动作**一直播，与程序中"参数合成的呼吸 / 摆头"冲突，二者只能取其一。建议用小写 `idle` 这类不会自动触发的名字，让它只在被绑定的触发点播放。

**表情**不用在这里加：`model3.json` 里已列出的会自动可用（当前的 Hotaru2024 有 **12 个 exp3**）；这里只列"目录里有、但没被引用"的表情文件。

实测（Hotaru2024）：`config.modelAssets` 为它注入了 `idle`、`sleep` 两个动作组；渲染层枚举为**表情 12 个 / 动作 2 个（组：idle、sleep）**，与设置窗里显示的一致。

实测（nyata）：该模型目录里有 `idle.motion3.json`，另有一个未被引用的 `帽子.exp3.json`；但 `config.modelAssets` 里**还没有**它的条目，所以渲染层枚举为**表情 0 个 / 动作 0 个**。想让它有动作/表情，按下面流程在这一页给它挂上即可。

### 换模型时这一页会自动重扫

在「模型」页切换模型后，本页会**自动重新扫描新模型的目录**，并丢弃上一个模型的候选与"已加载"标记（详见「四、切换模型 → 切换后…」），不需要关掉重开设置窗。

给一个模型配素材的完整流程：

1. 「模型」页切到该模型（桌宠立刻载入它，此时它还没有任何注入配置）；
2. 回到本页——候选已经是它自己目录里的文件了——勾选动作、填组名（表情同理），顺手到「触发方式与绑定」里配好触发；
3. 点**「保存并重新载入模型」**让注入生效，再到绑定表点「试播」确认。

> 第 3 步不能省：前两步只改了"注入配置"，桌宠手上还是上一份 `model3.json`。切模型时不需要点这个按钮（切模型本身就会重载），但**只改素材、不换模型时**必须点。

### 触发方式（可同时勾多种）

- **点击模型**：点一下模型就播，**播台词的行为不变，两者同时发生**。
- **待机随机**：每次出现待机台词时一起播。
- **快捷键**：填 `Alt+1`、`Ctrl+Shift+K` 这类写法。`Alt+1`、`ctrl+shift+m` 都会被归一成 Electron Accelerator。
- **台词触发**：选 `off`（不触发）/ 全部台词 / **只勾选特定几条台词**三种。

**快捷键范围**（全局设置，对整张绑定表生效）：

- **仅桌宠窗口内** —— 只有焦点在桌宠上时才响应，不抢系统键（默认，安全）。
- **全局** —— 系统级热键，任何程序前台都响应，注意别与其它软件冲突。

其它字段：**循环**（动作是否循环）与**保持时长(ms)**（`0` = 一直保持到下次触发）。每个绑定都有「试播」，可以立刻在桌宠上看到效果。

> 一套动作若同时命中多个绑定，**只会随机播其中一个**，避免两套动作抢同一批参数。

---

## 七、首跑常见现象与排错

把现象对照下表处理即可。绝大多数情况**不影响聊天**，只是渲染层问题。

### 0. 终端直接报错 / 弹窗，程序根本没启动

| 现象或报错原文 | 原因 | 正确写法 |
|---|---|---|
| 弹窗 `Unable to find Electron app at D:\live2d-companion"` / `Cannot find module 'D:\live2d-companion"'` | **`start.cmd` 里把应用路径写成了 `"%~dp0"`**。`%~dp0` 本身以反斜杠结尾，展开后是 `"D:\live2d-companion\"`，其中 `\"` 被 Electron 的参数解析器当成**转义引号**，路径末尾多出一个真实的 `"` 字符，于是找不到模块（注意报错文本里那个游离的引号） | 已在 `start.cmd` 中改为 `"%~dp0."`（展开为 `D:\live2d-companion\.`，末尾无反斜杠，解析干净）。自行改写启动命令时**不要让引号紧跟在反斜杠后面**：用 `"路径\."`，或先 `cd` 进目录再传 `.` |
| `Set-Location: 找不到接受实际参数"/d"的位置形式参数` | 把 CMD 的 `cd /d` 写进了 PowerShell | `Set-Location D:\live2d-companion` |
| `表达式或语句中包含意外的标记"start"` / `UnexpectedToken` | 以引号开头的路径在 PowerShell 里不能直接作为命令，它被当成了字符串字面量 | 前面加调用运算符：`& "C:\...\npm.cmd" start` |
| `Electron failed to install correctly, please delete node_modules/electron and try installing again` | `node_modules\electron\path.txt` 缺失（postinstall 被中断未写入） | 手动补写：`"electron.exe" \| Out-File "D:\live2d-companion\node_modules\electron\path.txt" -Encoding ascii -NoNewline` |
| `TypeError: Cannot read properties of undefined (reading 'whenReady')` at `main.js` | 环境变量 `ELECTRON_RUN_AS_NODE` 非空，electron.exe 退化成纯 Node，`require('electron')` 只返回一个路径字符串 | 正常情况不需要你处理：`main.js` 检测到这种状态会**自己去掉该变量并重新拉起自己**（自愈），所以从哪启动都能恢复。若自愈也失败，才会打印这条报错——此时手动清空该变量（`$env:ELECTRON_RUN_AS_NODE = ""`）再启动 |
| `'rem' 不是内部或外部命令`、`'it' 不是内部或外部命令`、命令行被拦腰截断成半截 | **`.cmd` 文件里出现了中文（非 ASCII 字符）**。cmd.exe 解析 `.cmd` 用的是当前 **OEM 代码页（本机 936/GBK）**，而不是 UTF-8，且在任何 `chcp` 生效之前就已经读完了。中文句号 `。` 的 UTF-8 编码以字节 `0x82` 结尾，而 `0x82` 在 GBK 里是**双字节首字节**，会把后面的 `\r` 一起吃掉 → **两行被合并成一行**，整段脚本被切碎 | **`start.cmd` / `start-debug.cmd` 必须保持纯 ASCII + CRLF 换行**，注释和提示文字一律用英文。需要中文说明请写在 `.md` 文档里。校验方法：文件字节中不应有 `> 127` 的字节 |

### 1. 窗口根本没弹出 / 程序一闪而过

- **双击 `start.cmd` 后命令行一闪而过、托盘也没有粉色图标**：先确认 `start-debug.cmd` 里显示的是什么。
  - 如果报的是「`'rem' 不是内部或外部命令`」这类**命令被截断**的错误 → 是 `.cmd` 文件编码问题（文件里混入了中文）。成因与修法见上表最后一行。**不要往这两个 `.cmd` 文件里写中文**。
  - 如果报的是 electron 相关错误 → 改用 `start-debug.cmd` 双击启动，它把真实报错留在窗口里，把那段文字或截图发回即可。也可以直接打开同目录的 `app.log` 看启动记录（启动时间、electron 版本、静态服务是否起来、窗口与托盘是否创建）。
  - 正常情况下 `app.log` 里应当看到连续的 4 行：`boot` → `static server on http://127.0.0.1:18765/` → `browser window created (… skipTaskbar=true)` → `tray icon ready`。缺哪一行，问题就在哪一步。
- **重复双击导致多个实例抢端口 `18765`**：`main.js` 现已加单实例锁，第二个实例会自动把已有窗口提到前台并退出，不会再因此崩。若仍怀疑端口被别的程序占用，改 `config.json` 的 `serverPort`（如 18766）再试。
- 终端反复打印下面两行、窗口始终不出现：
  ```
  ERROR:gpu_process_host.cc(997) GPU process exited unexpectedly: exit_code=1
  FATAL:gpu_data_manager_impl_private.cc(448) GPU process isn't usable. Goodbye.
  ```
  **这是 Chromium 渲染进程沙箱在当前进程上下文里初始化失败**，不是显卡问题（本机实测显卡是 RTX 3080，正常）。
  表征很隐蔽：渲染进程连 `about:blank` 都加载失败（`ERR_FAILED -2`），`webgl`、`gpu_compositing` 全部为 `disabled_off`，页面白屏。
  **处理**：`app/config.json` 里把 `"chromiumSandbox"` 设为 `false`（当前默认值）。`main.js` 会在 app ready 之前声明 `--no-sandbox`，对所有启动方式生效。
  实测加上之后 GPU 合成、WebGL 1/2 立即恢复（ANGLE + Direct3D11），模型能正常渲染。本程序只加载本机内容（自建 127.0.0.1 静态服务 + 本机模型目录），不访问任何远程页面，因此默认关闭沙箱；如你的环境沙箱正常，可把它改回 `true`。
- 先看启动它的那个终端有没有报错（尤其是 `EADDRINUSE` / `listen EACCES`）。
- 本程序先在 `127.0.0.1:18765` 起一个本地静态服务，再创建窗口。**端口被占用会导致窗口永不弹出**。改 `config.json` 的 `serverPort` 为其他空闲端口（如 18766）后重试。
- 确认 `node_modules\electron\dist\electron.exe` 确实存在（安装是否真的完成）。

### 2. 有窗口，但模型区一直"正在加载模型…"或显示"模型加载失败：…"
说明聊天外壳正常、Live2D 没起来。按概率排序：
- **依赖 UMD 路径写错**（本轮踩中三个）：
  - pixi v6 的 UMD 在 `dist/browser/pixi.min.js`，`dist/` 根目录没有这个文件 → 页面里 `PIXI` 直接 undefined。
  - 用 `pixi-live2d-display/dist/index.min.js` 全量包会先注册 Cubism 2 运行时，缺 `live2d.min.js` 时抛出 `Could not find Cubism 2 runtime` 并中断，导致 `PIXI.live2d.Live2DModel` 注册不上。**应改用 `dist/cubism4.min.js`**。
- **Cubism Core 版本太旧**：报 `csmReviveMocInPlace is failed. The Core unsupport later than moc3 ver:[4]. This moc3 ver is [5]`。
  npm 上的 `live2dcubismcore` 停在 1.0.2（Core 4.2.2），只支持 moc3 v4；Hotaru2024 的 `.moc3` 是 **v5**。项目已在 `app/vendor/live2dcubismcore.min.js` 放了一份取自 Live2D 官方 CDN 的 Core 5.1.0，`index.html` 引用的是这一份，**不要改回 `node_modules` 里那份**。
- **静态服务把请求拦了**：打开开发者工具（见第 5 点）看 Network。若 `/models/...` 返回 **403**，是 `main.js` 里 `safeJoin` 的目录穿越校验把正常请求误判了（`config.json` 里的正斜杠路径与 `path.resolve` 的反斜杠不一致，`startsWith` 比对失败）。已修，比较前先 `path.resolve` 归一化并转小写。
- **依赖请求全部 404**：检查 `/node_modules/...` 是否能打开。`main.js` 曾把路由前缀切错（`slice(1)` 保留了 `node_modules/`，拼成 `node_modules\node_modules\...`），以及漏了 `/app/*` 路由（`index.html` 用的是 `/app/` 前缀）。均已修。
- **模型路径写错**：确认 `config.json` 的 `modelUrl` 指向存在的 `*.model3.json`；路径大小写敏感（`Hotaru2024` 首字母大写）。

### 3. 状态栏显示"链路未就绪 · 未检测到 WorkBuddy 本地服务（127.0.0.1:9418）…"
先说明这一条**不是错误弹窗、也不影响模型**，它只是状态栏在报链路状态，程序会每 8 秒自动重试。

- 桥接依赖**本机正在运行的 WorkBuddy 桌面端**（它在 `127.0.0.1:9418` 暴露 ACP 服务）。请确认 WorkBuddy 桌面端已启动；它没起来时这条提示会一直挂着，起来后自动连上并改成「链路已建立 · WorkBuddy ACP 在线」。
- **早期版本这里显示的是「自动连接失败：Failed to fetch」**，看不出任何原因。根因是跨域：页面来自 `127.0.0.1:18765`，直接 `fetch` 别的端口 `127.0.0.1:9418`，上游没带 CORS 头时 Chromium 会在网络层直接拒绝，`fetch` 只抛一个含糊的 `TypeError: Failed to fetch`。现已改为**由主进程同源代理**（页面请求 `/api/v1/acp*`，主进程转发到 9418），并把真实失败原因翻译成中文返回。
- 想单独确认端口状态，可直接访问探活端点（返回 JSON，无副作用）：
  ```powershell
  curl.exe -s http://127.0.0.1:18765/api/v1/acp/health
  # {"ok":false,"code":"ECONNREFUSED","message":"未检测到 WorkBuddy 本地服务（127.0.0.1:9418）。请确认 WorkBuddy 桌面端正在运行。"}
  ```
- 若你的环境里该端口不是 9418（版本/配置不同），用 PowerShell 查：
  ```powershell
  Get-NetTCPConnection -State Listen | Where-Object { $_.OwningProcess -in (Get-Process -Name WorkBuddy).Id } | Select-Object LocalPort
  ```
  把查到的端口填进 `config.json` 的 `acpBaseUrl`（如 `http://127.0.0.1:新端口`）。
- 该接口免鉴权即可 `connect`，无需任何 token / API key。
- 若想回到"直连、不经代理"，把 `config.json` 的 `useAcpProxy` 设为 `false`（仅当上游已配置 CORS 时才可行，否则又会退回 `Failed to fetch`）。

### 4. 模型加载了但完全不动
Hotaru2024 磁盘上的 `model3.json` **没有 Motions 段**（VTube Studio 是另行驱动同目录 `idle.motion3.json` / `sleep.motion3.json` 的），所以只靠模型文件本身，它是一张静止立绘。
现在有**两条**动作来源，可以同时用：

1. **模型自带动作** —— 在设置「表情与动作」分页把 `idle.motion3.json` / `sleep.motion3.json` 关联进来（当前已配好）。程序在服务层返回 `model3.json` 时动态补写 `Motions` 引用，引擎就能加载它们了。想验证是否生效：看 `app.log` 里的 `[assets] 表情 N 个 / 动作 M 个（组：…）`。
2. **参数合成的待机动作** —— `live2d-loader.js` 内置"呼吸 + 轻微头部摆动 + 眼球微动"。不想开就把 `config.json` 的 `idleMotion` 设为 `false`。

> ⚠️ 两者会抢同一批参数。若你在设置里把动作组名填成 `Idle`（大写），引擎会把它当**自动循环的待机动作**一直播，和参数合成的待机打架 —— 请用小写组名（见第六节）。

说话时的口型（lip-sync）走 `model.internalModel.coreModel.setParameterValueById('ParamMouthOpenY', v)`；注意 `Live2DModel` 上**没有** `setParamValue`，早期代码用 `typeof === 'function'` 保护所以不报错，但也完全不生效。

### 5. 怎么看控制台日志（最关键）
Electron 窗口内按 **F12** 或 **Ctrl+Shift+I** 打开开发者工具，看 **Console**（JS 报错）和 **Network**（模型/接口请求）。首跑现象一律以这里为准，把报错贴回即可精准收口。

### 6. 一键自检（不用开 F12）
项目自带 `tools\selfcheck\`，它会用一个独立实例把页面真正跑一遍，直接给出结论。
**用法**：先启动主程序，再双击 `tools\selfcheck\run.cmd`（或执行
`& "D:\live2d-companion\node_modules\electron\dist\electron.exe" "D:\live2d-companion\tools\selfcheck\."`）。

控制台与 `selfcheck-result.json` 会给出：
- 资源是否有 4xx / 页面 JS 报错；
- 模型是否加载成功（`live2d-hint` 是否隐藏）；
- **待机动作是否真的在动**（隔 2 秒截两帧做像素比对）；
- **布局与关键 CSS 是否生效**：body 内边距、`#stage` 是否 `position: absolute`、渐隐 mask、灯带描边的 `mask-composite` 是否为 `exclude`、各元素尺寸（body/frame/stage/live2d/chat/bar）；
- **灯带是否内外对称**（`ringInsetLayers` ≥ 4）与**鞘层是否已移除**（`::after` 的 `content` 为 `none`）—— 这两条直接对应"边缘硬边"；
- **挡位断言：背景模式切换是否真的改变了渲染参数**（读 `backgroundColor` 在 透明 ↔ 毛玻璃 之间是否往返）；
- 底栏按钮文案与链路状态文本、记录框首条消息。

截图两张：`selfcheck-shot.png`（透明底）、`selfcheck-glass.png`（毛玻璃底）。

**另外三个针对本轮新功能的检查脚本**（都在 `tools\selfcheck\`，仍要先启动主程序）：

| 脚本 | 作用 |
|---|---|
| `run-settings-check.js` | 起主程序 + 跑 `probe-settings.js`，断言设置窗 9 个分页 / 5 个宽控件真的渲染出来。结果写 `probe-settings-result.json` |
| `probe-settings.js` | 单独跑设置窗探针（需主程序已在运行，走 `run-probe.js`） |
| `check-user-audio.js` | 端到端验证 `/api/user-audio` 白名单：未引用 / 非音频 / 缺参数 / 已引用 / 大小写 / 其它文件 六个用例 |
| `probe-model-sync.js` | **自足**探针（自带只读静态服务 + IPC 桩，不必起主程序）：驱动真实交互，断言切模型时「台词」与「表情与动作」两页自动同步（24 项） |

用法：

```powershell
$NODE = 'C:\Users\Administrator\.workbuddy\binaries\node\versions\22.22.2-3\node.exe'
& $NODE "D:\live2d-companion\tools\selfcheck\run-settings-check.js"
& $NODE "D:\live2d-companion\tools\selfcheck\check-user-audio.js"
```

> 改自检脚本时注意两个坑（都踩过）：
> 1. **隐藏窗口的 `capturePage()` 常返回"过期帧"**——窗口不可见时合成器不按需重绘。要断言状态请优先用 `getComputedStyle` 之类的"计算值"判断，截图只作观感参考。
> 2. **读计算值时要注意 transition**：`#window-frame` 上有 `transition: background-color .28s`，在同一帧里读会拿到过渡起点。测量前先把 `style.transition = 'none'`。另外 `::before` 上的样式必须用 `getComputedStyle(el, '::before')` 取，问元素本身只会得到初始值。
>
> 写新探针时再注意三点（本轮新踩）：
> 3. **`BrowserWindow` 必须在 `app.whenReady()` 之后再建**，否则直接报 `Cannot create BrowserWindow before app is ready`。
> 4. **探针页面若要用 `window.desktopPet`**，建窗时必须挂 `preload.js`，否则 `settings.js` 会走 `if (!api)` 分支、整页显示"主进程接口未就绪"。
> 5. **`capturePage()` 偶尔返回 0 字节**（本轮与 `settings-extra-shot.png` 都出现过）。截图不是断言依据，别因为它失败就判定功能坏了。

---

## 八、config.json 字段速查

| 字段 | 含义 | 当前值 |
|---|---|---|
| `serverPort` | 本地静态服务端口（同时是 ACP 代理的入口） | `18765` |
| `acpBaseUrl` | WorkBuddy ACP 地址 | `http://127.0.0.1:9418` |
| `useAcpProxy` | 经主进程同源代理访问 ACP（推荐 `true`，避开跨域导致的 `Failed to fetch`） | `true` |
| `acpRetryMs` | 链路未就绪时的自动重试间隔（毫秒） | `8000` |
| `startupBackground` | 首次启动的背景模式：`"clear"` 透明底 / `"glass"` 毛玻璃底（点过按钮后以 `localStorage` 为准） | `clear` |
| `modelServeBase` | 模型库根目录。**留空 = 用程序内 `app\models`**（当前就是留空） | `""` |
| `modelUrl` | 模型入口（相对 `/models/`） | `/models/Hotaru2024/hotaru2024.model3.json` |
| `cubismCore` | 仅作记录；实际由 `app/vendor/` 那份 core 决定 | `cubism3` |
| `window` | 窗口宽高（px）。其中每边 28px 是透明内边距（给外发光留空间），窗体实际尺寸 = 窗口 − 56px | `384 × 564` |
| `chromiumSandbox` | Chromium 渲染进程沙箱。默认 `false`（见排错第 1 点） | `false` |
| `idleMotion` | 待机动作（呼吸 + 轻微摆动） | `true` |
| `lipSync` | 口型开关 | `true` |
| `autoConnectChat` | 启动即连 WorkBuddy | `true` |
| `lineProfile` | **模型 → 台词档** 的绑定表，键是模型相对路径。未列出的模型落到 `default` | `{"Hotaru2024/hotaru2024.model3.json":"hotaru"}` |
| `modelAssets` | **给模型动态补写的 exp3 / motion 引用**（服务层返回 `model3.json` 时注入，不改磁盘文件）。键是模型相对路径，值为 `{motions:[{group,file,loop,fadeIn,fadeOut}], expressions:[{name,file}]}` | 为 Hotaru2024 注入 `idle`、`sleep` 两组动作 |
| `assets.hotkeyScope` | 绑定热键的作用范围：`"window"` 仅桌宠窗口内 / `"global"` 全局 | `window` |
| `assets.bindings` | 表情 / 动作的触发绑定表。每条含 `kind`(`motion`/`expression`)、`group`+`index` 或 `target`、`hotkey`、`onClick`、`onIdle`、`onLine`(`off`/`all`/`pick`)、`lineRefs`、`loop`、`holdMs` | 1 条演示绑定（`Alt+1` 播 idle 动作） |
| `ttsLang` | **按语言**指定的 TTS 引擎与音色（`cn`/`jp`/`en`）。供"语音来源 = TTS"的台词与整档默认值使用 | `en` 用 `edge` + `en-US-AriaNeural`，`cn`/`jp` 留空 |

> **这几项都由设置窗写入，一般不用手改**。其中 `modelAssets` 的特殊之处：改完**不会自动重载模型**，需在「表情与动作」分页点「保存并重新载入模型」才生效（避免编辑过程中模型反复重载）。

> `config.json` 由页面（`fetch('/config.json')`）与主进程各读一次：页面读的是**同一份文件**，所以改完必须**重启程序**，没有热更新。

---

## 九、已知限制 / 待迭代

1. **内存占用偏高（约 2.3 GB）**：模型本身很重——`hotaru2024.moc3` 56 MB，另有 6 张 4096 贴图（合计约 50 MB PNG，解压后显存占用更大）。单实例稳定运行没有崩溃，但同时开两个实例会顶到 4.7 GB 并可能让渲染进程被系统杀掉。若嫌重，可改用贴图尺寸小一些的模型。
2. **模型自带动画已接入**（原为待办）：`idle.motion3.json` / `sleep.motion3.json` 没写进 `model3.json` 的 Motions 段，现由服务层在返回 `model3.json` 时**动态补写引用**（见第六节），已实测渲染层能枚举出 `表情 12 个 / 动作 2 个（组：idle、sleep）`。参数合成的呼吸 / 摆头待机动作仍保留，用于模型没有自带 idle 动作的场合。
3. **动态注入的"补写"只发生在服务层**：磁盘上的 `hotaru2024.model3.json.enc` 保持原样。若把模型库换到别的机器、或用别的工具直读该文件，是看不到 `Motions` 段的 —— 需要同样依赖本项目的主进程。
4. **一套模型动作组名冲突会互相抢参数**：若把动态注入的组名写成 `Idle`（大写），引擎会把它当自动循环待机动作，与参数合成的呼吸 / 摆头同时改同一批参数。请用小写组名（见第六节）。
5. **自录音频是引用原路径**：移动 / 改名 / 删除源文件后对应台词会播不出来（见第五节），需重新在设置里选一次文件。
6. **音律识别的浏览器采集路径曾有一个真 bug（已修）**：`app/js/music-tracker.js` 在每次起音时调用了一个**从未定义**的 `estimateBpm()`，抛出的 `ReferenceError` 被外层 `catch` 吞掉，导致 `tick()` 尾部（相位推进 / 起音相位微调 / `estimateTempo()` 调用）**全部被跳过** —— 表现为浏览器采集（麦克风 / 立体声混音）模式下 BPM 永不更新、日志刷 `节拍检测异常：estimateBpm is not defined`。已删除该悬空调用（BPM 统一交给 `estimateTempo()` 的频谱通量自相关 + 投票）。**注意：本机默认走"系统回环"侧车路径，那条路径不在 `tick()` 里，所以这个修复无法在本机端到端复现，需要切到浏览器采集设备后确认日志不再刷该报错。**
7. **TTS 音色列表是静态白名单**：`tts/engines.py` 里 `EdgeTTSEngine.VOICE_LIST` 列出的音色（现含中文普通话 / 台湾 / 香港、日语、英语美/英，共 38 项）经 `edge_tts.list_voices()` 实查确认存在。若微软新增 / 下线音色，需手工同步该列表；填了不存在的音色会在合成时报错。
8. 聊天文本抽取按 `session/update` 通知递归取 `text` 字段；若某次回复结构特殊导致内容缺失，把 SSE 数据贴回，调 `workbuddy-bridge.js` 的 `_extractText` 即可。
9. 桥接会真实驱动 WorkBuddy 的 agent（与在 WorkBuddy 里对话等价），请勿在对话里发送会触发不可逆操作的指令。
10. 置顶状态目前无 UI 切换按钮，需在 `main.js` 改。
11. **毛玻璃模式不会模糊桌面**：窗体这一层的 `backdrop-filter` 采样不到"窗口背后的桌面"，只能生效于页面内部（聊天面板 / 底栏模糊身后的模型）。要让桌面真正被糊化需要系统级 acrylic/mica（Electron 的 `setBackgroundMaterial`），本项目未启用——因为本项目窗口是 `transparent: true`，与系统材质叠加容易把透明窗口变成不透明黑块，且无法在本机验证。毛玻璃模式目前是"半透明提亮"的效果。
12. **模型放大后被窗口裁切时是渐隐而非硬切**：`#stage`（上下）与 `#live2d`（左右）各挂一个 24px 渐隐 mask，定义在 `--fade-v` / `--fade-h`（6 段 stops 近似 smoothstep）。代价是贴近窗口边缘的 24px 内模型会被稍微淡掉；想更锐利就减小 stops 的像素值，但模型超出窗口时会重新看到直边。
13. **宠物对话附着在当前 WorkBuddy 会话上**：实测 `session/new` 在一条连接里**第一次**调用返回的往往是 WorkBuddy 当前正在用的会话，之后才新建；用 `cwd`（`config.json` 的 `acpCwd`）并不能可靠地隔离出独立会话。表现是 `session/prompt` 的流会先推一遍整段会话历史（一次实测 29 万字符 / 1400+ 事件，首尾各有一个 `_meta['codebuddy.ai/historyReplay']` 标记）。桥接已按标记把回放段整段丢弃，但你发的消息仍会进入主会话。彻底隔离需要先探明"如何在连接内稳定拿到新会话"，尚未定论。

---

## 十、文件结构

```
D:\live2d-companion\
├─ 黑叶萤桌宠.exe        # ← 双击即启动（最省事）：PyInstaller 启动器，拉起的是项目根当前源码
├─ _launcher_build\     #   上述 exe 的源码与打包配置（launcher.py / 黑叶萤桌宠.spec）
├─ start.cmd            # 与 exe 等价的 .cmd 启动方式（维护须知见「二、如何启动」）
├─ start-debug.cmd      # 同款启动但保留命令行窗口，报错可见
├─ main.js              # Electron 主进程：透明窗口 + 本地静态服务 + 路由 + 沙箱开关 + ACP 同源代理 + 托盘
│                       #   + /models/* 静态服务（含 .enc 解密、model3.json 动态注入）+ /api/user-audio 白名单
│                       #   + 设置窗 IPC（模型扫描 / 台词档 / 音色 / 绑定 / 试播）+ globalShortcut 热键
├─ preload.js           # contextBridge 暴露 desktopPet.*（窗口控制 / 配置读写 / 模型 / 台词 / 素材绑定）
├─ package.json
├─ app.log              # 每次启动的关键步骤日志（双击没反应时先看它）
├─ launcher_debug.log   # 启动器日志：每次双击 exe 的 spawn 时间与 pid
├─ window-bounds.json   # 窗口位置/大小的记忆（退出时写回）
├─ 运行说明.md           # ← 本文档
├─ docs\
│  └─ 方案-模型切换与表情动作.md   # 三大功能的设计方案（含当时待拍板的 8 个点）
├─ app\
│  ├─ config.json       # ← 主要改这里（模型、端口、ACP、背景、待机、台词档绑定、modelAssets、绑定表、ttsLang）
│  ├─ index.html        # 页面骨架（窗体 → 舞台 → 聊天面板 → 底部控制栏）+ Live2D 依赖的 UMD 引用
│  ├─ styles.css        # 霓虹灯带 / 玻璃面板 / 舞台渐隐 / 滚动条 / 按钮
│  ├─ settings.html     # 设置窗骨架（顶部选项卡 + 内容区 + 提示气泡）
│  ├─ settings.css      # 设置窗样式（.row 网格；`.row:has(.wide-ctrl)` 让宽控件独占整行）
│  ├─ settings.js       # 设置窗逻辑：SCHEMA 定义 9 个分页，makeCtrl 按 type 分派到各 make* 构建器
│  ├─ screen-debug.html # 屏幕追踪调试面板（托盘「调试 → 屏幕追踪调试」：画出捕捉画面与识别到的运动物体）
│  ├─ music-debug.html  # 音律识别调试面板（托盘「调试 → 音律识别调试」：能量/起音阈值/起音时刻、连续相位与每拍包络、以及**实际写进模型**的参数值）
│  ├─ brain-chooser.html# 启动时的「选择聊天大脑」窗（本地 / 云端 / WorkBuddy + 人设定制）
│  ├─ ollama-setup.html # 本地模型安装向导（托盘「本地模型安装向导…」：检测 → 装 Ollama → 起服务 → 拉模型 → 启用）
│  ├─ models\           # ← 模型库（当前含 Hotaru2024，资源为 .enc 加密；留空 modelServeBase 时用它）
│  ├─ data\lines\
│  │  ├─ *.wav          # 预生成的离线语音（click_cn_00.wav 等）
│  │  └─ profiles\      # ← 台词档：<档 id>.json（hotaru / default …）
│  ├─ assets\tray.png   # 托盘图标
│  ├─ vendor\
│  │  └─ live2dcubismcore.min.js  # ← 官方 Core 5.1.0，支持 moc3 v5（勿删）
│  └─ js\
│     ├─ app.js              # 界面逻辑：对话、链路重试、背景切换、问候语、台词档载入、绑定分发
│     ├─ live2d-loader.js    # Live2D 加载 + 待机动作 + 口型 + 拖动/缩放/锁定/记忆 + 表情/动作播放与挂起
│     ├─ chat-backends.js    # 「大脑」抽象层（本地 Ollama / 云端 OpenAI 兼容 / WorkBuddy）+ 情绪标签清洗
│     ├─ brain-chooser.js    # 选择大脑窗逻辑；探测本机 Ollama 失败时给出「一键安装」入口
│     ├─ ollama-setup.js     # 安装向导逻辑：驱动检测/安装/起服务，并读 NDJSON 流算拉模型进度
│     ├─ music-tracker.js    # 音律识别（BPM / 节拍驱动闭眼跟拍）
│     ├─ screen-tracker.js   # 屏幕运动追踪（抓屏 → 运动物体 → 视线/头部跟随）
│     └─ workbuddy-bridge.js # ACP 桥接（同源代理 / 直连两路；已实测 9418 协议）
├─ tts\                 # TTS 侧车（Python，127.0.0.1:18766）
│  ├─ tts_server.py     # HTTP 侧车：/health /voices /tts
│  └─ engines.py        # 各引擎实现（cosyvoice / indextts / edge / sapi / tone）；Edge 音色白名单在此
├─ _icon_work\          # 图标制作脚本与中间产物（app_icon.ico 的生成过程）
├─ dist\                # 打包产物
│  ├─ 黑叶萤桌宠-Setup-0.2.2.exe        # ← 当前 Windows 安装包（NSIS，不包含 Ollama，见「本地模型」一节）
│  ├─ 黑叶萤桌宠-0.2.2-mac-arm64.zip    # ← 当前 macOS 包（Apple Silicon，解压即用）
│  ├─ 黑叶萤桌宠-0.2.2-mac-x64.zip      # ← 当前 macOS 包（Intel，解压即用）
│  ├─ win-unpacked\                # 免安装版（主程序名 HotaruDesktopPet.exe）
│  │                               #   其 resources\app\ 是打包那一刻的副本（asar 关闭，明文目录）
│  └─ _mac_src\                    # Electron 官方 darwin 二进制（组装 mac 包的原料，可删）
│
│  约定（用户要求）：**每次打包都要三件套一起出**（Windows + mac arm64 + mac x64），
│  并**把上一个版本的包删掉**，让 dist\ 里只留当前版本。详见「十一、打包与发布」。
├─ tools\
   ├─ selfcheck\        # ← 一键自检（见排错第 6 点）
   │  ├─ run.cmd / run-probe.js  # 起一个 Electron 探针脚本（会显式清掉 ELECTRON_RUN_AS_NODE）
   │  ├─ m.js                    # 主自检脚本：连已在运行的服务，断言计算值 + 截图
   │  ├─ selfcheck-result.json
   │  ├─ selfcheck-shot.png      # 透明底截图
   │  ├─ selfcheck-glass.png     # 毛玻璃底截图
   │  ├─ probe-settings.js       # 设置窗探针：断言 9 个分页 / 5 个宽控件真的渲染出来
   │  ├─ run-settings-check.js   # 编排器：起主程序 → 等静态服务 → 跑设置窗探针 → 收尾
   │  ├─ probe-ollama-setup.js   # 本地模型向导探针：自带静态服务 + 桩掉 IPC 与 pull 流，
   │  │                          #   覆盖 5 个环境场景的状态机与进度/取消/报错分支（不下一个字节）
   │  ├─ run-ollama-wizard-check.js # 编排器：A 页面级探针 + B 真主程序 CDP 端到端（含真网络探源）
   │  ├─ check-user-audio.js     # 端到端验证 /api/user-audio 白名单（含正反用例）
   │  ├─ probe-model-sync.js     # 切模型时「台词」/「表情与动作」两页是否自动同步（自足探针）
   │  └─ final-verify.js         # 静态断言汇总（node 直接跑）
   └─ pack\             # ← 打包用（见「十一、打包与发布」）
      ├─ build-mac.py            # 手工组装 macOS「解压即用」zip（Windows 上代跑 electron-builder）
      ├─ verify-mac.py           # 校验 mac zip：create_system / 符号链接 / 权限位 / plist / 载荷 / 无 config.json
      ├─ preflight-github.js     # 上传前敏感文件预检（只读，报告写 preflight-report.txt）
      └─ release-config.js       # ⚠ 已不再需要：config.json 已不进包（保留作历史与本机路径差异记录）
```

### 本轮（模型切换 / 台词三语 / 表情动作）实测结论

在**设置窗**里用探针断言渲染结果（不是断言"函数存在"），实测：

- **9 个选项卡**全部渲染：通用 / 模型 / 台词 / 表情与动作 / 语音 / 屏幕追踪 / 视线跟随 / 音律识别 / 高级，共 73 行控件。
- 三个新分页的 5 个宽控件（`.model-lib` / `.lp-picker` / `.line-editor` / `.asset-link` / `.asset-binder`）都在，且 `:has(.wide-ctrl)` 布局规则**确实命中**：`.ctrl` 的 `grid-column` 为 `1/-1`、`grid-row` 为 2，行模板由基线的 `1fr auto auto` 变为 `1fr auto`。
- 台词列表渲染出 **10 张台词卡**（对应 `hotaru` 档的 10 条点击台词）；绑定表渲染出 **1 张绑定卡**（`config.json` 里的演示绑定）。
- 素材关联扫到 **2 个动作文件**，且两个都标为「已加载」。
- 主程序日志确认：`[assets] 表情 12 个 / 动作 2 个（组：idle、sleep）`、`[lines] 台词档=hotaru 点击 10 条 / 待机 10 条`、`[assets] 触发绑定 1 条，快捷键范围=window`。
- 三语 TTS 实测合成成功：`zh-CN-XiaoxiaoNeural` 23472 B、`ja-JP-NanamiNeural` 32112 B、`en-US-AriaNeural` 29376 B（均 `audio/mpeg`）。
- `/api/user-audio` 六个用例全部符合预期（见第五节表格）。
- **切模型时两页自动同步**（本轮新增，24 项断言全过）：从 `Hotaru2024/hotaru2024.model3.json` 切到未绑档的 `nyata/nyata.model3.json` 后，台词页自动回退 `default` 并给出说明、素材页自动重扫出 nyata 自己的 `idle.motion3.json` 与 `帽子.exp3.json`；切回 Hotaru2024 时同样同步回来（档回到 `hotaru`、动作回到 idle + sleep）。

复现命令（主程序可先不起，编排器会自己起）：

```powershell
node D:\live2d-companion\tools\selfcheck\run-settings-check.js   # 设置窗渲染断言
node D:\live2d-companion\tools\selfcheck\check-user-audio.js     # 自录音频白名单
node D:\live2d-companion\tools\selfcheck\run-probe.js tools\selfcheck\probe-model-sync.js   # 切模型同步（自足，无需主程序）
```

> 探针窗口是 `show:true` 的（唯一能拿到当前帧的方式），运行时会短暂闪一下。设置窗探针会自己注册 `pet:*` 的 IPC 桩并把调用次数记进 `probe-settings-result.json`，因此**不会**动到你的真实配置。

> **当前实测结论**：Electron 30.5.1 / Chromium 124 / Node 20.16.0；本地静态服务 18765 正常；页面资源全部 200、无 JS 报错；PixiJS 6.5.10 跑在 WebGL 2（ANGLE + Direct3D11，RTX 3080）；Cubism Core 5.1.0；Hotaru2024 模型加载成功并渲染出画面（贴图 RGBA、模型自身**无**不透明底板，已用"品红底实验"验证），待机动作生效（隔 2 秒两帧像素不一致）；布局 312×443 落在 344×475 视口内、底栏 5 个按钮完整可见；灯带描边 `mask-composite` 计算值为 `exclude`；背景模式切换往返正常（`rgba(0,0,0,0)` ↔ `rgba(10,14,22,0.34)`）。
> 以上均可由 `tools\selfcheck\` 复现。

---

## 十一、打包与发布

### 1. 先分清三样东西

> **约定（用户要求，2026-09-22 起）：每次打包 = 三个产物一起出 + 删掉上一个版本的包。**
> Windows 安装包、mac arm64、mac x64，一个都不能少；打完把旧版本的
> `*Setup-<旧版本>.exe` / `*.blockmap` / `*-mac-<旧版本>.zip` 删掉，让 `dist\` 只留当前版本。
> （`dist\_mac_src\` 是组装原料，不是产物，保留。）

| 东西 | 是什么 | 它跑的是 |
|---|---|---|
| 根目录 `黑叶萤桌宠.exe` | PyInstaller **启动器**（8.1 MB） | **项目根当前源码** —— 改完源码双击它就是最新版 |
| `dist\黑叶萤桌宠-Setup-*.exe` | NSIS **安装包** | 打包那一刻的静态副本 |
| `dist\win-unpacked\` | 免安装版目录 | 同上，静态副本 |
| `dist\黑叶萤桌宠-*-mac-<arch>.zip` | macOS 解压即用包 | 同上，静态副本（组装方式见第 4 节） |

**改源码不会影响已经打好的包，装好的包也不会跟着源码变。**

### 2. 打包前必做：不用切配置了（config.json 已不进包）

**2026-09-22 起改掉了机制**：`package.json → build.files` 加了 `"!app/config.json"`，
**运行时配置不再进包**，安装版首启直接走 `main.js` 的内置默认：

- 模型路径 = 程序自带的 `app/models/Hotaru2024`（`.enc` 加密版，约 172 MB，已在包内）
- 聊天大脑 = 默认**云端** + 弹「选择聊天大脑」窗
- 屏幕追踪 / 音律识别 = 默认关；`chromiumSandbox` 缺省即 no-sandbox

也就是说，原来那套「apply 发布配置 → 打包 → restore」**整段都省了**，
`tools\pack\release-config.js` 从此不再需要（脚本保留，只当历史与本机路径差异的记录）。

这同时修掉了一个真实故障：以前 `apply` 忘做或漏了某台机器，使用者装完可能
**打不开（空白窗）**或**被直接扔进没装好的本地模型**，还找不到地方改。

> ⚠ 但别忘了 mac 包：它的排除项在 `build-mac.py` 里，是另一份配置，必须同步（见下面第 4 节）。

### 3. Windows 安装包

```powershell
# ① 先清输出目录！electron-builder 会自己递归删旧的 dist\win-unpacked，
#    实测能删 4 分钟只删掉八成然后卡住，看着像"打包永远跑不完"。
#    用 robocopy 镜像空目录秒清（见第 6 节）。
# ② 再打包
node D:\live2d-companion\node_modules\electron-builder\out\cli\cli.js --win --x64 --publish never
```

- 产物：`dist\黑叶萤桌宠-Setup-<version>.exe`（0.2.2 实测 265.7 MB、1 分 33 秒）
- 默认安装目录由 `build\installer.nsh` 钉在 `D:\HotaruDesktopPet`（向导里仍可改）。
- NSIS 二进制已在本地缓存（`%LOCALAPPDATA%\electron-builder\Cache\nsis`），**可离线打包**。
- 已设 `win.signAndEditExecutable: false`（无证书），可绕开 `winCodeSign` 的 symlink 权限崩溃。

> 注意：**不能**用 `node_modules\.bin\electron-builder.cmd`——本机安全策略不允许从命令行拉起 `cmd.exe`，直接调 `cli.js` 即可。

### 4. macOS「解压即用」包

`electron-builder --mac` 在 Windows 上会被它自己拒绝：

```
⨯ Build for macOS is supported only on macOS
```

所以 mac 包由 `tools\pack\build-mac.py` **手工组装**：

```powershell
# ① 下载对应架构的 Electron 官方二进制，放进 dist\_mac_src\
#    https://npmmirror.com/mirrors/electron/30.5.1/electron-v30.5.1-darwin-arm64.zip
#    https://npmmirror.com/mirrors/electron/30.5.1/electron-v30.5.1-darwin-x64.zip
# ② 组装（用带 Pillow 的解释器，本机是 D:\index-tts\.venv，Pillow 11.3.0）
D:\index-tts\.venv\Scripts\python.exe D:\live2d-companion\tools\pack\build-mac.py arm64
D:\index-tts\.venv\Scripts\python.exe D:\live2d-companion\tools\pack\build-mac.py x64
```

- 产物：`dist\黑叶萤桌宠-<version>-mac-<arch>.zip`，解压即得 `黑叶萤桌宠.app`。
- 单次组装约 35 秒；该架构约 300 MB（0.2.2 实测：arm64 299.6 MB / x64 305.0 MB）。

**装完必须用校验脚本过一遍**（只读 zip，不解压，约 10 秒）：

```powershell
D:\index-tts\.venv\Scripts\python.exe D:\live2d-companion\tools\pack\verify-mac.py
```

它按技能清单逐项断言：`create_system == 3`、14 个符号链接的载荷是相对路径、
`Contents/MacOS/Electron` 权限 = `0o755`、`Info.plist` 的名称/版本/可执行名、
`electron.icns` 是有效容器、载荷文件齐全，以及 **`app/config.json` 不在包内**。
退出码非 0 表示有断言失败。

> ⚠ **两处白名单必须同步。** Windows 包的排除项在 `package.json → build.files`（`"!..."` 行），
> mac 包的排除项在 `build-mac.py` 的 `EXCLUDE_PREFIXES` —— 它们是两套独立配置。
> 2026-09-22 踩过：`"!app/config.json"` 只加到了 `build.files`，Windows 包验过是干净的，
> **但 mac 包仍然带着开发机配置**，等于同一个坑只修了一半、而漏的那半正好是发给 Mac 用户的。
> 所以每次打包都要**两个包都验一次**（Windows 看 `dist\win-unpacked\resources\app\app\config.json` 是否存在，
> mac 跑 `verify-mac.py`）。

**为什么不能"先解压再压缩"**：Electron 的 `*.framework` 靠 **14 个 Unix 符号链接**组织
（`Versions/Current -> A` 等），且 `Contents/MacOS/Electron` 需要可执行位。Windows 文件系统里
这两样都不存在（建 symlink 需要特权），一旦落盘再压缩必然丢失，解压出来的 app 根本起不来。

脚本的做法是**只在 zip 层面搬运**：读源 zip 的条目（symlink 条目的"内容"就是目标路径字符串，
权限位在 `external_attr` 里），改名后连同元数据一起写进目标 zip，一个字节都不落盘。

> **脚本里最关键的一行是 `zi.create_system = 3`。** Python 在 Windows 上新建 `ZipInfo` 时
> 该字段为 `0`，解压端会据此**忽略 Unix 权限位** —— 漏掉它整个包就是废的。
> 这是本次唯一必须靠"提前知道"才避得开的坑。

缺 Pillow 时脚本会跳过图标（沿用 Electron 默认图标），不影响能否启动。

### 5. 发布包的体积构成（Windows 安装包 278 MB）

| 部分 | 未压缩 | 说明 |
|---|---|---|
| `app\models\Hotaru2024` | 171.9 MB | `.enc` 已加密 ≈ 不可压缩，几乎 1:1 进包 |
| Electron 运行时 | ~200 MB | 压缩后约 80 MB |
| `node_modules` | 57.5 MB | 只有 `dependencies`（pixi.js / pixi-live2d-display / pinyin-pro） |
| `app\` + `tts\` | ~12 MB | 页面、脚本、台词档、TTS 侧车 |

> `app-builder-bin`(121 MB)、`typescript`(22 MB)、`7zip-bin`(12 MB) 看着很大，但它们都在
> `devDependencies` 里 —— **electron-builder 只打包 `dependencies`**，这些本来就不进包。
> 想进一步瘦身只能换更小的模型，或让用户自备模型库。

### 6. 打完请验证这几项

```powershell
# ⓪ 打包前：先清输出目录（否则 electron-builder 自己删旧的 win-unpacked 会卡住）
#    robocopy /MIR 用空目录镜像掉它，再删空壳；比逐文件删快一个数量级
$empty = Join-Path $env:TEMP '_empty_for_mirror'; New-Item -ItemType Directory -Force $empty | Out-Null
robocopy $empty 'D:\live2d-companion\dist\win-unpacked' /MIR /NFL /NDL /NJH /NJS /NC /NS /NP /R:0 /W:0
Remove-Item 'D:\live2d-companion\dist\win-unpacked' -Recurse -Force

# ① 安装包是不是真 PE（期望输出 4D 5A，即 MZ）
$fs=[IO.File]::OpenRead('D:\live2d-companion\dist\黑叶萤桌宠-Setup-0.2.2.exe')
$b=New-Object byte[] 2; $null=$fs.Read($b,0,2); $fs.Close(); ($b|%{$_.ToString('X2')}) -join ' '

# ② ★ 包内不该有 config.json（2026-09-22 起的规定，False 才是对的）
Test-Path dist\win-unpacked\resources\app\app\config.json

# ③ 关键文件在不在（三条都应为 True）
Test-Path dist\win-unpacked\resources\app\audio\audio_server.py
Test-Path dist\win-unpacked\resources\app\app\settings.js
Test-Path dist\win-unpacked\resources\app\app\models\Hotaru2024\hotaru2024.moc3.enc
Test-Path dist\win-unpacked\resources\app\app\ollama-setup.html
```

mac 包：**直接跑校验脚本**（只读 zip，和解压无关，约 10 秒）：

```powershell
D:\index-tts\.venv\Scripts\python.exe D:\live2d-companion\tools\pack\verify-mac.py
```

它覆盖：`create_system == 3`、符号链接数（**14**）且载荷是相对路径、
`Contents/MacOS/Electron` 权限 = **0o755**、`Info.plist`
（`CFBundleDisplayName` = 黑叶萤桌宠 / `CFBundleExecutable` = Electron / 版本号对得上）、
`electron.icns` 是有效容器、载荷文件齐全、**`app/config.json` 不在包内**。
组装日志 `<zip>.log` 里也会打出 `symlinks=14` 与 icns 字节数。

### 7. 已知差异与限制

- **两个平台的包都会带上 Windows 专属的 TTS 绝对路径**（`ttsPython` / `cosyvoicePython` 等）。
  别人机器上没有那些 venv 与 GB 级权重，TTS 会不可用 —— 预生成 wav 与自录音频不受影响。
  这是设计取舍：TTS 依赖外部 Python 环境，无法塞进安装包。
- **mac 包没有签名**。首次打开需**右键 → 打开**，或先执行
  `xattr -cr /Applications/黑叶萤桌宠.app`，否则会被 Gatekeeper 拦下。
- **mac 包未在真机验证过**。目前只证到"包结构 + Unix 元数据 + 内容完整性"这一层；
  实际启动、macOS 下的托盘表现、`globalShortcut` 热键（mac 需要辅助功能授权）都还没实机确认。
- `_mac_src\` 里那两个 darwin 二进制（合计约 198 MB）是组装 mac 包的原料，**可随时删除**，
  下次组装重新下载即可。

---

## 十二、聊天大脑（本地 / 云端 / WorkBuddy 三选一）

桌宠的"思考"部分被抽成了一个可替换的后端，三种随时切换。

### 1. 三种大脑是什么

| 大脑 | 跑在哪 | 需要什么 | 断网 | 隐私 |
|---|---|---|---|---|
| **本地模型** | 你自己的显卡（Ollama） | 装 Ollama + 下一个模型（约 4.7 GB） | 可用 | 聊天内容不出本机 |
| **云端接口** | 对方服务器 | 一个 API Key | 不可用 | 内容会发到对方服务器 |
| **WorkBuddy** | WorkBuddy 当前会话 | WorkBuddy 正在运行 | 不可用 | 走本机 ACP |

### 2. 启动时的选择窗

默认**每次启动都会弹**，让你选用哪个。窗口右下角有个「**下次不再询问**」：

- **不勾** —— 每次启动都问，但默认选中项 = 你上次用的那个。
- **勾上** —— 之后直接沿用上次的大脑，不再弹窗。

首次安装时默认指向**云端**（本地模型还没装，选了也用不了）。

选择窗里还有两块：

- **云端接口设置**：服务商预设（DeepSeek / 通义 / Kimi / 智谱 / OpenAI）+ 接口地址 + 模型名 + API Key，
  填完可以点「测一下」确认能不能通。密钥只写进本机的 `app/config.json`。
- **人设定制**：他叫什么、性别、怎么称呼你、自称什么、与你的关系、性格、语气、禁忌话题。
  改完点「确定」立即生效。

运行中想改：**托盘图标右键 → 切换聊天大脑…**

### 3. 人设文件

人设存在 `app/data/persona.json`，三种大脑共用同一份。默认设定：

> 黑叶萤，**男性**，身份是你的**助理**，称呼你「博士」，自称「萤」。

想恢复默认：把 `app/data/persona.json` 删掉，下次启动会自动重建。

### 4. 本地模型的冷启动（重要）

本地模型第一次推理要把约 4.7 GB 权重装进显存，**实测冷启动首字延迟约 53 秒**，
之后同一模型驻留期间降到 **33 毫秒**。所以：

- 选了「本地模型」后，程序会在启动时**自动预热一次**（状态栏会显示加载进度）。
- 配置里的 `chat.local.keepAlive` 默认 `30m`，即半小时不用才卸载。
  桌宠这种"半天说一句"的用法，如果按 Ollama 默认的 5 分钟，会反复触发冷加载。

如果没装 Ollama，**不用自己敲命令**：选择大脑窗里「本地模型」那张卡片会直接显示
「🔧 一键安装 Ollama →」按钮，点开就是**本地模型安装向导**（见第 6 节）。

### 5. Ollama 的生命周期：按需拉起，随桌宠退出

Ollama **不用你手动启动**，也不用设成开机自启：

- **选中「本地模型」时才拉起。** 选云端或 WorkBuddy 时它不会被启动，不占显存。
- **退出桌宠时一并关闭。** 但如果 Ollama **本来就在跑**（比如你命令行起的、
  或其它软件在用），桌宠只是"借用"，**退出时不会关它** —— 关一次桌宠就把别人的
  服务搞没，太糟了。判断依据是"这次是不是我们拉起来的"，日志里写得很明白：

  ```
  ollama: stopped (pid=47272)                              ← 我们起的，关掉了
  ollama: not spawned by us, leaving it running            ← 别人的，不动
  ```

- 关闭时按**进程树**杀。Ollama 会再派生一个 runner 子进程（真正占显存的是它），
  只杀主进程会留下孤儿继续吃显存，所以走 `taskkill /F /T`。

**模型目录必须传对**（最容易踩的坑）：模型默认装到 `~/.ollama/models`，
但本机装在 `D:\ollama-models`。启动时不指定目录的话，服务会去读那个空目录，
然后报「找不到模型」——看起来像没下载，其实是读错了地方。
程序按 `chat.local.modelsDir` → 环境变量 `OLLAMA_MODELS` → `D:\ollama-models`
的顺序取，取到哪个都会写进日志：

```
ollama: spawned (...\ollama.exe) modelsDir=D:\ollama-models
```

看到 `modelsDir=(default)` 却又找不到模型时，就在 config.json 里显式填
`chat.local.modelsDir`。

对外接口（调试用）：`pet:ollamaStart` / `pet:ollamaStatus` / `pet:ollamaStop`。

### 6. 本地模型安装向导（一键装 Ollama + 拉模型）

**安装包不包含 Ollama**（安装包本身 1.5 GB，模型还要另拉 4.7 GB，自带一套约 7.4 GB）。
所以新用户第一次想用本地模型时，向导会把这条路走完：

**入口**：托盘右键 →「本地模型安装向导…」；或选择大脑窗里「本地模型」卡片上的
「🔧 一键安装 Ollama」。两处打开的是同一个窗口（`app/ollama-setup.html`）。

**五步**（每步都能单独重来，中途关窗可续）：

| 步骤 | 做了什么 | 关键点 |
|---|---|---|
| ① 环境检测 | 查装没装 / 服务在不在 / 模型有没有 / 放哪 / 磁盘够不够 | 点「重新检测」可反复刷；模型目录在这儿改 |
| ② 安装 Ollama | 探 4 条下载源 → 挑最快的下 1.5 GB → 静默安装 | **不需要管理员、不弹 UAC**（安装器带 `/CURRENTUSER`） |
| ③ 启动服务 | 拉起 `ollama serve` 并等端口就绪 | **必须先于第 ④ 步**，否则 pull 会自己起服务然后超时卡死 |
| ④ 下载模型 | 拉模型并显示百分比/速度/剩余时间 | 走 NDJSON 流，支持取消；已下的部分会保留，再点续传 |
| ⑤ 启用 | 写入 `chat.local.model` 并把大脑切到本地 | 等于选择窗点确定，只是不关窗 |

**下载源**：`OllamaSetup.exe` 托管在 GitHub release，国内直连经常不通
（winget 也栽在同一处，报 `InternetOpenUrl() failed`）。向导会并发探活这四条并按延迟排序，
失败自动换下一条：

| 源 | 前缀 |
|---|---|
| gh-proxy 镜像 | `https://gh-proxy.com/` |
| ghfast 镜像 | `https://ghfast.top/` |
| ghproxy 备用镜像 | `https://mirror.ghproxy.com/` |
| GitHub 直连 | 无前缀 |

本机实测（2026-09-22）：**3/4 条通**，gh-proxy 349ms、直连 5305ms、ghfast 9921ms。
四条全不通时，页面会给「浏览器手动下载」按钮手动兜底（浏览器带系统代理，往往比程序内更通）。

**其他细节**：下完会比对 GitHub 给的 sha256 再安装（避免半截包被当成装完）；
关窗即取消任务（不会在看不见的地方继续吃流量），已下好的 `.part` 保留供续传；
安装完成后所有窗口会收到 `pet:ollamaReady`，选择大脑窗自动重新检测。

**接口**：`pet:ollamaDetect` / `pet:ollamaProbeSources` / `pet:ollamaInstall` /
`pet:ollamaInstallCancel` / `pet:ollamaOpenDownloadPage` / `pet:ollamaSaveLocalCfg` /
`pet:ollamaUseLocal`；进度事件 `pet:ollamaSetupProgress`。
自检：`node tools/selfcheck/run-ollama-wizard-check.js`（页面级 49 条 + 端到端 21 条）。

### 7. 问时间/日期：走「时钟快通道」，不经过模型

大模型训练完就"封片"了，它**不知道今天是几号、现在几点**。本地 7B 被问"几点了"时，
会照着训练数据里的年份编一个答案——看着像模像样，其实是错的。两条对策：

1. **system prompt 里注入本机现实时间**（每次发消息时重新取），所有与日期相关的问题
   都以它为唯一准绳；
2. **纯时间/日期问答直接查系统时钟回**，根本不调模型。既准，又省掉本地模型几秒的等待。

会被快通道接走的说法（举例）：

| 你说 | 回答 |
|---|---|
| 现在几点了 / 几点 / 现在时间 | 现在是 04:12，星期二。 |
| 今天几号 / 今天是什么日子 / 现在日期 | 今天是 2026年9月22日。 |
| 今天星期几 / 周几 | 今天是星期二。 |
| 明天几号 / 昨天几号 / 后天星期几 | 明天是 2026年9月23日。 |
| 今年是哪一年 / 现在是几月份 | 现在是 2026年。 / 现在是 9月份。 |
| 距离 2027年1月1日 还有多少天 | 距离 2027年1月1日 还有 101 天。 |

**不会**被接走（交回模型）："会议几点开始""什么时间出发合适""时间管理"这类问安排而非
问时钟的；以及"距离春节还有多久"（没有确定日期）。日期差只认**具体年月日**，
没写年份时按"今年、已过则明年"处理。

想让所有时间问题都走模型（比如为了测试），把 `chat.clockFastPath` 设成 `false`。

#### 6.1 节日快通道

中秋 / 春节 / 元宵 / 端午 / 国庆 / 元旦 这些**农历或固定节日**，小模型既不会算
（中秋是农历八月十五）又答得慢还答错。所以也走查表——一张核验过的公历日期表，
**秒回、且一定准**，星期几和"还有多少天"运行时现算。举例：

| 你说 | 回答 |
|---|---|
| 中秋节是哪天 / 几月几号 | 中秋9月25日，星期五。 |
| 今年中秋是星期几 | 中秋9月25日，星期五。 |
| 距离中秋还有多少天 | 中秋9月25日，还有 3 天。 |
| 明年中秋几号 | 中秋2027年9月15日，星期三。 |
| 春节是哪天 / 明年春节几月几号 | 春节2月17日，星期二。 / 春节2027年2月6日，星期六。 |
| 端午节日期 / 元宵是哪天 | 端午6月19日，星期五。 / 元宵3月3日，星期二。 |
| 国庆节还有多少天 / 元旦是几月几号 | 国庆10月1日，还有 9 天。 / 元旦1月1日，星期四。 |

**不会**被接走："中秋节有什么习俗""端午节快乐"这类不是问日期的；以及表格里
**没有收录的年份**（交回模型，并由下面的超时保护兜底）。

> 注：节日表只收录了有把握的年份（多源核对）。要支持更远的年份，在
> `chat-backends.js` 的 `FESTIVALS` 表里按 `{ 年: [月, 日] }` 续加即可。

#### 6.2 思考超时保护（防止"想太久"）

本地模型冷启动或遇到难缠的问题时，可能**十几秒都不吐第一个字**，界面就一直转圈。
加了看门狗：**从请求发出到第一个字，超过 `chat.thinkTimeoutMs`（默认 15000 毫秒 /
15 秒）还没动静，就直接放弃这次生成**，回一句敷衍的：

> 「博士，这个我一时答不上来，咱们换个话题？」

这样就不会再让你对着转圈干等。已经吐出过字的（哪怕只吐了一点）不会被判超时，
会正常显示。云端接口同理受这个超时约束。

想放宽/收紧：改 `chat.thinkTimeoutMs`（单位毫秒，设 `0` 或负数等于关闭保护）。

### 7. 相关的 config.json 字段

```json
"chat": {
  "backend": "cloud",          // local | cloud | workbuddy
  "askEveryStart": true,       // 是否每次启动都弹选择窗
  "clockFastPath": true,       // 时间/日期/节日问答走快通道、不调模型（设 false 全走模型）
  "thinkTimeoutMs": 15000,     // 思考超时保护：首个字超过这个时间(ms)就敷衍回复；<=0 关闭
  "local":  { "baseUrl": "http://127.0.0.1:11434", "model": "qwen2.5:7b-instruct-q4_K_M",
              "temperature": 0.8, "numCtx": 8192, "keepAlive": "30m",
              "modelsDir": "",        // 空=自动推断；找不到模型时在这里显式填，如 D:\\ollama-models
              "port": 11434 },
  "cloud":  { "baseUrl": "https://api.deepseek.com/v1", "model": "deepseek-chat",
              "apiKey": "", "temperature": 0.8, "maxTokens": 512 },
  "workbuddy": { "cwd": "" }
}
```

云端用的是 **OpenAI 兼容格式**（`/chat/completions`），所以 DeepSeek、通义千问、Kimi、
智谱、OpenAI 只是换地址和模型名，代码不用改。

### 8. 情绪标签

三种大脑都会被要求以情绪标签开头输出，例如：

```
[joy]博士，这事办妥了。
```

标签会被剥掉再显示，同时驱动她的五官表情（可用的标签见
`app/js/live2d-loader.js` 里的 `EMOTION_PRESETS`：neutral / focus / curious / joy /
sleepy / affection / shy / tease / serious / surprise / pout / smug / enjoy）。
小模型经常不守规矩，所以还有一层关键词兜底，认不出来就 neutral —— 不会报错。

### 9. 两条代理路由

渲染进程不直连外部服务，全部由主进程转发（和 WorkBuddy 的 ACP 同一套路）：

- `/api/ollama/*` → `http://127.0.0.1:11434/*`
- `/api/cloud/*` → 配置里填的 baseUrl（Authorization 头在主进程拼，不经过页面）
