# 更新笔记 · 黑叶萤桌宠

> **统计区间**：2026-09-20 11:20（首次打包安装程序） → 2026-09-21 08:42
> **对照基线**：`dist\win-unpacked\resources\app\`（2026-09-20 14:18 打包快照。项目 `asar: false`，所以这是一份可逐字节比对的明文副本）
> **写这份笔记的办法**：拿打包快照跟当前源码做全量比对（161 → 281 个文件，10 改 / 0 删 / 120 增），再用修改时间筛出"首次打包之后被动过"的 47 个文件，最后与各次工作日志逐条核对。**结论全部有据，不含推测。**

---

## 一、先讲三件结论性的事

1. **你双击项目根目录的 `黑叶萤桌宠.exe` 用的就是这份笔记里的全部内容。** 那个 exe 是 PyInstaller 启动器（源码 `_launcher_build\launcher.py`），自身不含应用代码，每次启动都从项目根加载 `main.js` 与 `app\`。所以源码一改，双击它就是最新版。
2. **`dist\` 里的安装包停在 2026-09-20 14:18。** 这份笔记里 **14:23 之后的改动一个都不在那个安装包里** —— 14:23 你定了"不主动重打包"的约定，之后所有功能都只留在源码里。
3. **规模**：相对打包快照，逻辑文件 **改 9 个**（`main.js` +40.4 KB、`live2d-loader.js` +25.3 KB、`app.js` +18.5 KB、`preload.js` +7.4 KB 等），**新增 6 个**（设置面板三件套、音律识别、2 份台词档），另加一个 `audio\` 侧车。合计新增代码约 **150 KB**。
   （原始比对还报了 `package.json` 有差异，但那是 electron-builder 打包时会**重写一份裁掉 devDependencies 的副本**所致，不代表源码升级，故不计入。）

---

## 二、总表：哪些是新东西，安装包里有没有

| 功能块 | 首次可用 | 在 09-20 14:18 安装包里？ |
|---|---|---|
| 跨平台（macOS）适配 | 09-20 12:26 | ✅ 有（14:18 那次重建已含） |
| 屏幕运动追踪 | 09-20 14:15 | 🟡 有旧版（算法是"会追鼠标"的那版） |
| **设置面板**（独立窗口，9 个分页） | 09-20 15:04 | ❌ 无（快照里 `app\settings.*` 根本不存在） |
| 鼠标追踪开关 | 09-20 15:23 | ❌ 无 |
| 屏幕追踪：选屏 + 「标识屏幕」 | 09-20 15:37 | ❌ 无 |
| **视线跟随**（4 参数可调） | 09-20 17:34 | ❌ 无（快照 `config.json` 无 `gaze` 段） |
| **额外追踪参数**（自选参数跟视线动） | 09-20 17:53 | ❌ 无 |
| **音律识别**（原「音乐点头」） | 09-20 18:5x | ❌ 无（快照里无 `music-tracker.js`、无 `music` 段） |
| WASAPI 逐端点回环侧车 | 09-21 02:1x | ❌ 无（`audio\` 整个目录都没进打包范围，见第六节） |
| **模型自由切换**（免重启） | 09-21 06:5x | ❌ 无 |
| **台词 · 三语言 · 语音来源** | 09-21 07:0x | ❌ 无 |
| **表情与动作**（exp3 / motion3 关联与触发） | 09-21 07:2x | ❌ 无 |
| 切模型时台词页 / 表情动作页自动同步 | 09-21 08:0x | ❌ 无 |

---

## 三、分项详述

### 3.1 设置面板（全新）

以前所有参数都写在 `app\config.json` 里、改完要重启。现在是**一个独立的设置窗口**，schema 驱动渲染，**改即存、即时下发**。

- **打开方式**：托盘菜单「设置…」，或主窗底栏的「设置」按钮（这个按钮原来是「隐藏」，隐藏窗口的功能改由托盘菜单承担）。
- **9 个分页**：通用 / 模型 / 台词 / 表情与动作 / 语音 / 屏幕追踪 / 视线跟随 / 音律识别 / 高级。
- **每个参数都带一个「?」**，鼠标悬停或键盘聚焦即显示作用说明与参考范围。
- **不需要重启的参数当场生效；需要重启的会挂一条「↻ 重启生效」横幅**，不让你猜。
- 新增文件：`app\settings.html`（805 B）、`app\settings.css`（12 KB）、`app\settings.js`（94 KB）。
- 挂载点：`main.js` 的 `openSettings()`；渲染进程用 `preload.js` 暴露的 `desktopPet.*` 桥接。

### 3.2 屏幕运动追踪

- **修算法**：原来用帧差找最大运动块，结果**把鼠标光标本身当成了运动最剧烈的目标** —— 这就是你反馈的"开了屏追、灵敏拉高也还是在追鼠标"的真根因（跟优先级无关）。现改为 **16×9 网格 + 4 邻接连通域分析**，只取"运动像素最多的连通块"质心（画面内容运动铺满多格，光标只占 1~2 格），并加绝对像素下限 `MIN_MOTION_PX = 120` 过滤光标与单点噪点。
- **默认参数一并重新标定**：`fps` 15→20、`threshold` 22→5、`motionMin` 0.012→0.003、`spread` 0.85→0.4、`releaseFrames` 10→30。
- **新增「鼠标追踪」开关**（`config.mouseFollow`）：给你一个排查/隔离手段。三态优先级 = 屏幕运动追踪 → 鼠标追踪 → 视线回正。
- **新增「追踪屏幕」选择 + 「标识屏幕」按钮**：下拉列出本机所有显示器；点"标识"会在每块屏上弹一个大编号（与 Windows 显示设置一致，当前选中项蓝底高亮 + 「✓已选」），2.6 秒自动消失。

### 3.3 视线跟随（鼠标追踪与屏追共用的参数）

原来这些值全是写死在代码里的常量，现在可调：

| 参数 | 含义 | 当前值 |
|---|---|---|
| 视线平滑 | 瞳孔跟随的平滑时间常数 | 300 ms |
| 眼球幅度 | 乘到 `ParamEyeBallX/Y` 的倍率 | 1.0 |
| 头部幅度 | 乘到头部左右/俯仰偏转的倍率 | 2.0 |
| 头部平滑 | 头部跟随的平滑时间常数 | 200 ms |

### 3.4 额外追踪参数

除了眼球与头部，还能**自己挑别的 Live2D 参数**跟着视线动（参数下拉来自模型真实枚举，如 `ParamBodyX/Y/Z`、`ParamAngleZ` 等）。每行可设方向（X / Y）、幅度（0–8）、翻转。

你当前挂了 3 条：`ParamBodyAngleX`(X, 1.5)、`ParamBodyAngleY`(Y, 1.0)、`ParamBodyAngleZ`(X, 1.0)。

### 3.5 音律识别（原「音乐点头」）

放歌时角色跟着节拍摇头闭眼。这块迭代最多，最终形态：

- **两套方案可分别开关**：
  - **A 方案 · 连续律动** —— 用自适应峰值包络把音量归一化成幅度系数，连续轻晃（`aSpeed` 默认 65，可调 40–200）。静音时幅度归零。
  - **B 方案 · BPM 锁定正弦** —— 估出 BPM 后用正弦连续摆动，把相位往鼓点最高点轻推 0.35，**与鼓点同频同相但保持平滑**（不是每拍抽一下）。
- **律动落到哪些参数**：
  - 头部俯仰 → `ParamAngleY`（低头方向，负值）
  - 身体摆动 → **`ParamBodyAngleZ`**（特意挑这个：`ParamBodyAngleX` 已被呼吸占用，抢了会打架）
  - 眼睛 → 缓动闭到设定值（≈700 ms 时间常数），接管期间**完全不眨眼**
  - 表情 → 新增 **`enjoy`（陶醉）预设**：眼开 0.30 / 眉微抬 / 嘴形微笑 / 轻微歪头，交给已有的 240 ms 缓动情绪系统；**说话时让位给台词情绪**
- **BPM 估计算法重写**（旧算法"测什么歌都 100 出头、同一首歌还漂"）：改为 **频谱通量 → 去均值归一化自相关 → 谐波梳状（lag + 0.5·2lag + 0.25·3lag）→ 120 BPM 对数高斯先验解倍频歧义 → 抛物线插值取亚帧精度 → 投票直方图取最密集簇**（投票是"不漂"的关键），并带换歌检测。分析帧率 33 ms → 16 ms。
  - **离线验证**：70 / 90 / 100 / 110 / 120 / 128 / 140 / 150 BPM **误差全部 ≤ 0.6**，三次复跑稳定。
  - 已知可接受偏差：174 BPM 会判成 87（经典八度歧义），按半速点头观感自然。
- **`maxTempo` 上限折半**：估测值超过上限就不断除以 2（120→60、128→64），**折半作用在投票之前**，保证观测面板显示值 = 实际律动速度。
- **监听设备下拉**，分三组：
  - 「系统声音：默认输出设备的混音（推荐，就是你在听的音乐）」
  - 输入设备（麦克风 / 立体声混音等，可直接采集）
  - **输出设备（逐端点回环，可任选）** —— 这条是新做的原生 WASAPI 逐端点采集，见下
- **观测面板**（只读）：实时显示"是否检测到音乐 / 在跑 A 还是 B / 测得 BPM"。
- **总开关关闭时，所有子控件自动置灰**。

**原生 WASAPI 逐端点回环侧车（新）**：Chromium/Electron 的回环 API 只能抓 Windows **默认输出设备**那一路，不能按端点选。为了让你能指定任意音箱/耳机，新增了一个 Python 侧车：

- 文件 `audio\audio_server.py`（16.7 KB），端口 = 主服务端口 + 3（即 18768），随主程序启动、退出时清理。
- 用 `soundcard` 库枚举输出端点并 `include_loopback=True` 采集，能对**非默认端点**开回环。
- BPM 分析与 `music-tracker.js` **同构移植**（同一套算法），并在 `audio\venv` 里装了 **librosa 0.11.0** 做二次估计：**librosa 有值即用 librosa，否则回退自研自相关**。
  - **精度实测**（合成瞬态 click track）：70→69.8 / 90→90.7 / 100→99.4 / 120→120.2 / 128→129.2 / 150→152.0 / 175→175，单帧误差 ≤ 2.6，比自研更准。
  - 采集线程启动前用静音 dummy 预热 numba JIT，不阻塞 `/start` 响应。

### 3.6 模型自由切换（免重启）

- 设置 → 「模型」页：填模型库目录 → 「扫描」→ 下拉选模型 → 「载入模型」，**当场热切换，聊天不中断**。
- **换库目录也不用重启**：服务层每次请求才去求值库根（`modelServeBase()`），不再缓存。
- 服务层还会**按需给 `model3.json` 补写引用**：模型目录里的 `motion3.json` / `exp3.json` 若没写进 `model3.json` 的 `Motions` / `Expressions` 段，返回时会动态补上（磁盘上的原文件保持不动）。
- **新模型默认台词档 = 通用话术全走 TTS**（不是空白），见下节。

> ⚠️ 你当前 `modelServeBase` 填的是 `...\VTube Studio_Data\StreamingAssets\Live2DModels\Hotaru2024`（指到了**某个模型目录**而不是库根），所以模型下拉里只有 1 个候选。填到 `...\Live2DModels` 就会一次列出全部 38 个模型。

### 3.7 台词 · 三语言 · 语音来源

- **台词档**机制：`app\data\lines\profiles\` 下每个模型一份 JSON，可新建多个档并绑定给不同模型。
  - `hotaru.json`：点击 10 条 / 待机 10 条（由原来的 `click-lines.json` + `idle-lines.json` 迁移，中文/日语沿用已录制好的 wav，**行为与改动前一致**）
  - `default.json`：通用档，点击 8 条 / 待机 6 条，中日英齐全，全部走 TTS —— 换到没绑档的模型时自动回退到它
- **三语言**：显示语言与语音语言各自可选中 / 日 / 英；某语言没有文本时，语音会回退用中文的语音来源。
- **每条台词可单独指定语音来源**（三语言逐条配置）：
  1. **预生成文件** —— 项目内已有的 wav
  2. **自录音频** —— 选本机任意音频文件，**引用原路径、不复制**（经 `/api/user-audio` 白名单校验：只放行台词档里引用过的路径、只放行音频扩展名）
  3. **TTS 合成** —— 可选引擎 + 音色，支持试听
- **edge 引擎修复**：`edge_tts` 原来只装在另一个 venv 里，导致 `/health` 里 `edge(unavailable)`。已在 TTS 侧车实际使用的 `D:\cosyvoice_src\.venv` 里装上 edge-tts 7.2.8（实测合成成功）。
- **音色列表从 8 个扩到 38 个**：原来只有 8 个 `zh-CN`，而配置和台词档里本就在用 `ja-JP-NanamiNeural` / `en-US-AriaNeural` —— **下拉里根本选不到**。现覆盖 zh-CN 8 / zh-TW 3 / zh-HK 3 / ja-JP 2 / en-US 17 / en-GB 5，**每一项都经 `edge_tts.list_voices()` 实查确认存在**。三语实测：cn 23472 B、jp 32112 B、en 29376 B。

### 3.8 表情与动作（模型自带的 exp3 / motion3）

设置 → 「表情与动作」页，两块：

**(a) 素材关联**
- 「扫描目录」列出当前模型目录里所有 `*.motion3.json` / `*.exp3.json`
- 勾选要用的 → 填动作组名 → 选是否循环 → **「保存并重新载入模型」**
- 写进 `config.modelAssets`，由服务层在返回 `model3.json` 时注入

你当前配的：`idle.motion3.json` → 组 `idle`、`sleep.motion3.json` → 组 `sleep`，都带 `loop: true`、`fadeIn/fadeOut 0.5`。渲染层实测枚举结果：**表情 12 个 / 动作 2 个（组：idle、sleep）**。

**(b) 触发绑定** —— 一条绑定 = "什么条件下，播哪个表情/动作"：

| 可设项 | 取值 |
|---|---|
| 类型 | 动作（按组名 + 序号）或 表情（按名字/序号） |
| 触发方式 | 点击模型 / 待机随机 / 快捷键 / **台词触发** |
| 台词触发的范围 | `off` 不触发 · `all` 任意台词都触发 · `pick` **只在你勾选的那几条台词时触发** |
| 快捷键范围 | **全局**（关掉窗口也响应）或 **仅窗口内** |
| 其它 | 循环开关、保持时长、单条试播 |

你当前有一条演示绑定：动作 `idle#0`、快捷键 `Alt+1`、保持 12 s，快捷键范围 = 仅窗口内。

### 3.9 切模型时，台词页与表情与动作页自动同步

这是你最后提的那条需求。**根因不在主程序** —— 起真机冒烟时日志已经写着 `台词档=default 点击 8 条 / 待机 6 条`，说明桌宠本身早就正确回退了，**是设置窗没跟着走**，三个原因：

1. 「载入模型」成功后**只改了状态栏那行文字**，没更新本地模型上下文，也没通知别的分页；
2. 「台词」页的模型键与档 id 是**构造时算一次的快照**；
3. 「表情与动作」页还揣着**上一个模型目录的扫描结果**。

改法：引入设置窗级的「当前模型」广播，四个分页各自订阅，换模型统一走一个入口。

- 台词页：重算绑定键与档 id；新模型没绑档就回退 `default`，并在页面上写明"该模型未绑定台词档，已回退到通用档"。
- 表情与动作页：丢弃旧扫描结果，用新模型重新扫描。
- 触发绑定页：重渲染候选，顺带修掉"换台词档后『哪些台词』多选列表不更新"。
- 主进程新增转发：主窗上报素材后直接推给设置窗，替掉原来 `setTimeout(…, 2500)` 猜时机。

### 3.10 界面与文案

- 底栏「隐藏」→「设置」；「锁」→「锁定」。
- 「音乐点头」全项目改名 **「音律识别」**（含托盘菜单、设置页、控制台日志；配置键 `music.*` 与代码标识符保持不变）。
- 滑条读数改为**固定宽度 + 可直接键入的数字框**（原来读数宽度随数值变化，会把滑条挤动）。
- 设置窗新增 `.disabled` 灰态样式，总开关关闭时子控件置灰。

---

## 四、一并修掉的缺陷（18 条，全部有实测依据）

### 音律识别

| # | 现象 | 根因 | 处理 |
|---|---|---|---|
| 1 | 测什么歌 BPM 都 100 出头，同歌还漂 | 旧算法把拍间隔**倍频折叠**进 60–180（等于往区间中心推），且每来一票就 EMA 更新 | 重写为频谱通量 + 自相关 + 谐波梳状 + 120 BPM 先验 + 投票直方图 |
| 2 | 显示"未检测到音乐"却仍在晃 | `isPlaying()` 用**起音**判定，晃动幅度用**音量**；安静段落没有起音但确实在放歌 | 改用音量判定（`level > 0.08`，1.5 s 窗口） |
| 3 | 开启音乐模式后窗口"凭空消失"、进程还在 | `getUserMedia({audio:{desktop}, video:false})` 在 Windows 上**直接崩渲染进程**（崩在 resolve 之前，所以日志里成功/失败都没有）；透明窗一崩就是视觉上的消失 | 改为**同时请求 video**，拿到流后立刻停掉视频轨；并给 `render-process-gone` 加日志 |
| 4 | 闭眼怎么调都不生效 | `model3.json` 有 **EyeBlink 组**且**没有 Motions** → pixi 每帧都跑 `eyeBlink.updateParameters` **覆盖写**眼参数；而且模型更新挂在 `Ticker.shared`、我们的写入挂在 `app.ticker`，**眨眼跑在我们的写入之后** | 加载后设 `internalModel.eyeBlink = null`（本模型无自带动画，不损失任何东西） |
| 5 | 眼睛在半闭处"抽搐" | 眨眼波形叠加在固定闭眼值上 | 音乐眼接管期间**完全不眨**；眨眼间隔 1.5–5 s → 2.2–6.5 s |
| 6 | 开着音律识别但没放歌也半闭眼，且 `eyeClose` 调不动 | 开总开关就切 `enjoy` 预设（眼开 0.30），`_express` 每帧写眼；而 `eyeClose` 只在"playing"分支才覆盖眼值 | `_express` 对眼参数加 `if (!this._musicEmo)` 守卫 —— 音律识别开启时表情层完全不碰眼睛 |
| 7 | 点头轴错了（变成左右转头） | nod 脉冲写到了 `ParamAngleX`（yaw），应为 `ParamAngleY`（pitch） | 改对，并单独记录 Y 基线 |
| 8 | 关掉音乐后身体僵在最后摆动的角度 | `ParamBodyAngleZ` 没有其它系统接管 | 关闭时显式归零 |
| 9 | 浏览器采集模式下 BPM 永不更新 | `music-tracker.js` 每次起音都调用**从未定义**的 `estimateBpm()`，抛出的 `ReferenceError` 被 `tick()` 外层 `catch` 吞掉 → **`tick()` 尾部（相位推进 / 起音相位微调 / `estimateTempo()` 调用）整段被跳过** | 删除该悬空调用，BPM 统一交给 `estimateTempo()` |
| 10 | 识别到音乐后**没有连贯地晃动，只是偶尔抽动一下** | 早期为治"快歌下自由正弦被读成抽搐"，把渲染层的连续项整个删掉，改成只由起音触发"每拍一次性点头"——于是动作**完全由起音决定**：起音一稀疏就整段静止。更糟的是追踪器仍在算 `getSwing()`（连续相位），渲染层**根本没人读它**（`_musicTick` 里已成孤儿），`swayStrength` 也就失去作用 | `_musicTick` 恢复**分层叠加**：连续项（`swing`，速度由 A 方案 `aSpeed` / B 方案锁定 BPM 决定，被 `maxTempo` 折半限制）+ 每拍重音项（`nodEnv`/`nodSway`）。身体以连续项为主体（×4），头部以重音为主体（×5）+ 小幅连续项（×2）。日志实证：起音间隔 2.5 s 时旧写法 **94% 的帧身体完全静止**、新写法仅 4%；幅度上限仍为 `摆幅×(4+1.5)×amp`，不会累积放大 |

> 📌 **更正一条旧记录**：之前的工作日志里写过"`estimateBpm` 已随函数改名 `estimateTempo` 修复"，**那是错的**。它不是改名，是悬空调用。"重启后无报错"只是因为默认走系统回环侧车那条路径，BPM 在 Python 侧算，**根本不经过 `tick()`**。

### 屏幕追踪 / 视线

| # | 现象 | 根因 | 处理 |
|---|---|---|---|
| 11 | 开着屏追却在追鼠标 | 帧差把**鼠标光标本身**当成最大运动块 | 16×9 网格 + 连通域 + 绝对像素下限 |
| 12 | 持久化的视线/音乐参数启动不生效，"再改一个参数才反映" | `app.js` 启动只推了音乐 tracker 初值，**漏推 `setGazeCfg` / `setParams`** | 启动末尾补齐 |
| 13 | 「额外追踪参数」整页不显示 | `settings.js` 的 schema **漏挂** `{key:'gaze.extra', type:'extraParams'}`（函数、样式、分发分支都在，就是没挂上去） | 补挂。`node --check` 全绿也发现不了这类漏接 |
| 14 | 参数下拉永远只有兜底候选 | `getAllParamIds()` **根本不存在**；且 `_hasParam` 被定义了两次，后者覆盖前者，写-读回环那段成了死代码 | 补 `getAllParamIds()`；前者改名 `_paramWritable()` 并加缓存 |
| 15 | 不开音乐时角色偶尔把头发呆转向别处 | 走神（wander）调度：无人互动时每 18–40 s 随机游离 | 整段移除；保留自然待机微动作 |

### 设置面板 / 其他

| # | 现象 | 根因 | 处理 |
|---|---|---|---|
| 16 | 拖动滑条时滑条左右抽动 | 读数 `<span>` 宽度随数值变化（"90 BPM"→"150 BPM"），把固定 190 px 的滑条整体挤动 | 改为固定宽滑条 + 可编辑数字框 + 固定单位 |
| 17 | 切模型后台词页 / 表情动作页不跟着换 | 见 3.9 的三条 | 引入模型上下文广播，四处订阅 |
| 18 | edge 引擎不可用、日语英语音色选不到 | edge-tts 只装在另一个 venv；`VOICE_LIST` 仅 8 个 zh-CN | 装 edge-tts 7.2.8；列表扩到 38 项（实查） |
| 19 | —— | `settings.js` 绑定渲染里的死代码 `b.triggers = b.triggers || {}`，会往每条保存的绑定里塞无用的 `triggers:{}` | 删除。已确认真正的触发字段是 `onClick`/`onIdle`/`onLine`/`lineRefs`，三处一致，无 schema 错位 |

### 一个工程教训（值得单记）

**同一条消息里对同一个文件发多个编辑，只有最后一个会落盘**，而工具仍回报成功。这已经导致过两次"改了却像没改"：一次是「额外追踪参数」没挂进 schema，一次是「音律识别」改名有一批没落盘（用户截图里 tab 还是旧名字）。

对策已固化为纪律：**同一文件一次只改一处；改完立刻回查目标字符串是否真在文件里，以回查结果为准，不信"编辑成功"的返回。** 另需注意：语法检查全绿 ≠ 改动生效（丢掉的代码本身也是合法的），也查不出"函数写好了但没被引用"。

---

## 五、仍未验证 / 已知限制

1. **动作与表情的实际观感没验证过** —— 只证到"服务层注入 → 引擎枚举/加载"这一层（`表情 12 个 / 动作 2 个`），**"是否真的动起来、挂起时间窗是否与情绪层或参数合成的呼吸/摆头打架"必须肉眼确认**。
2. **BPM 修复无法在本机端到端复现** —— 默认走系统回环侧车，不经 `tick()`。请把「监听设备」切到麦克风/立体声混音，确认日志不再刷 `节拍检测异常`。
3. **音律识别依赖外部音频环境** —— WASAPI 逐端点需要 `audio\venv` 里的 `soundcard`；BPM 精算需要 `librosa`。缺任一都会优雅降级，不是崩溃。
4. 屏追的"选屏"与"标识"编号按索引对齐，**前提是桌面捕获源顺序与显示器枚举顺序一致**（Windows 下通常成立）；若不一致会错位，需改按几何坐标匹配。
5. 内存占用约 2.3 GB（模型本身重：`moc3` 56 MB + 6 张 4096 贴图）；同时开两个实例会顶到 4.7 GB 并可能被杀。
6. 自录音频是**引用原路径**，源文件移动/改名/删除后那条台词就播不出来。
7. 动态注入的 `Motions`/`Expressions` **只发生在服务层**，磁盘上的 `.enc` 保持原样 —— 用别的工具直读该文件看不到这些引用。
8. 动作组名**不要用大写 `Idle`**，引擎会把它当自动循环待机动作，与参数合成的呼吸/摆头抢参数。
9. TTS 音色是静态白名单（38 项），微软增删音色需手工同步。

---

## 六、重新打包之前，有 3 件事要先处理

> 这三条只影响"打安装包发给别人"，**不影响你现在双击 exe 使用**。

1. **`package.json` 的打包范围缺 `audio/`** ⚠️
   `build.files` 只列了 `main.js` / `preload.js` / `package.json` / `app/**` / `tts/**` / `node_modules/**`。
   而音律识别的 WASAPI 侧车是 `audio\audio_server.py`，`main.js` 用 `__dirname + 'audio'` 去找它 —— **打包后这个文件不会存在**，WASAPI 逐端点回环功能会在安装版里静默失效（其它采集方式不受影响）。
   需要往 `files` 里加 `"audio/audio_server.py"`，同时排除 `audio/venv`（**418 MB**，而且 venv 是绝对路径、搬到别的机器也不可用）。

2. **TTS 侧车依赖本机外部环境**
   `ttsPython = D:\index-tts\.venv\...`、`cosyvoicePython = D:\cosyvoice_src\.venv\...`、模型权重大小以 GB 计。装机到别的机器时这些路径不存在，**TTS 会不可用**（预生成 wav 与自录音频不受影响）。要在别人的机器上可用，需要单独给 TTS 做一份部署方案。

3. **`modelServeBase` 建议改填模型库根目录**
   现在指到 `...\Live2DModels\Hotaru2024`，模型下拉只有 1 项；填到 `...\Live2DModels` 就有 38 项。

**顺带两处过时文档**（不影响使用，但发出去会被看到）：
- `SETUP.md` 还写着 ACP 端口 `9418`（已过时，现在是自动发现、hint 10925）
- `运行说明.md` 第 91 行仍写"标题栏（Live2D · WorkBuddy + 聊天/**锁**/重置/**隐藏**）"，实际已是「聊天 / 锁定 / 重置 / **设置**」

---

## 七、附：文件级改动清单

### 7.1 内容有改动（相对 09-20 14:18 快照）

| 文件 | 大小变化 | 最后修改 |
|---|---|---|
| `main.js` | 62,299 → **102,724** B（+40.4 KB） | 09-21 08:06 |
| `app\js\live2d-loader.js` | 80,015 → **105,282** B（+25.3 KB） | 09-21 07:33 |
| `app\js\app.js` | 30,501 → **48,981** B（+18.5 KB） | 09-21 07:33 |
| `preload.js` | 2,789 → **10,215** B（+7.4 KB） | 09-21 07:30 |
| `app\js\screen-tracker.js` | 8,040 → **12,493** B（+4.5 KB） | 09-20 15:41 |
| `app\config.json` | 1,549 → **3,535** B（+2.0 KB） | 09-21 08:42 |
| `tts\engines.py` | 26,110 → **27,291** B（+1.2 KB） | 09-21 07:39 |
| `app\index.html` | 3,394 → **3,452** B（+58 B） | 09-21 04:16 |
| `app\styles.css` | 21,305 B（同尺寸，内容有改） | 09-21 04:16 |

`config.json` 新增的段：`modelServeBase`/`modelUrl` 改由设置面板管、`lineProfile`、`modelAssets`、`assets`（含 `hotkeyScope` 与 `bindings`）、`ttsLang`（三语言引擎+音色）、`mouseFollow`、`gaze`（含 `extra`）、`music`（含 `enableA`/`enableB`/`aSpeed`/`maxTempo`/`swayStrength`/`audioSource`）、`screenTrack.screenIndex` / `screenTrack.enabled`。

### 7.2 新增文件

| 路径 | 大小 | 作用 |
|---|---|---|
| `app\settings.js` | 93,755 B | 设置窗口全部逻辑（schema 驱动 + 9 个分页） |
| `app\settings.css` | 12,030 B | 设置窗口样式 |
| `app\settings.html` | 805 B | 设置窗口骨架 |
| `app\js\music-tracker.js` | 30,650 B | 音律识别（采集 + BPM + 律动状态） |
| `app\data\lines\profiles\hotaru.json` | 12,102 B | Hotaru2024 台词档（点击 10 / 待机 10） |
| `app\data\lines\profiles\default.json` | 8,258 B | 通用台词档（点击 8 / 待机 6，全 TTS） |
| `audio\audio_server.py` | 16,660 B | WASAPI 逐端点回环侧车（+ librosa BPM） |
| `docs\方案-模型切换与表情动作.md` | 12,027 B | 本轮三大功能的设计方案与 8 项决策记录 |
| `tools\selfcheck\run-probe.js` | — | 无头 Electron 探针编排器（通用） |
| `tools\selfcheck\probe-settings.js` | 12,253 B | 设置窗口渲染断言 |
| `tools\selfcheck\run-settings-check.js` | 3,226 B | 设置窗口自检编排 |
| `tools\selfcheck\check-user-audio.js` | 4,991 B | 自录音频白名单端到端验证 |
| `tools\selfcheck\probe-model-sync.js` | 20,617 B | 切模型同步验证（自足探针） |
| `tools\selfcheck\final-verify.js` | 5,647 B | 31 项静态断言汇总 |
| `tools\settings-extra-probe.js` | 8,873 B | 「额外追踪参数」渲染验证 |

### 7.3 文档

- `运行说明.md` — **大幅重写**（54.5 KB）：原有 8 节重排为 10 节，新增「四、切换模型（设置里点一下，免重启）」「五、台词 · 三语言 · 语音来源」「六、表情与动作（模型自带的 exp3 / motion3）」；补齐 config 新字段表、文件结构树、自检脚本清单、排错条目。
- `docs\方案-模型切换与表情动作.md` — 新增，本轮三大功能的设计基线。
- `SETUP.md` — **未更新**（见第六节）。

---

---

## 八、0.2.0 打包记录（2026-09-21 12:00）

### 产物

| 平台 | 文件 | 大小 |
|---|---|---|
| Windows | `dist\黑叶萤桌宠-Setup-0.2.0.exe` | 278,566,698 B |
| macOS（Apple Silicon） | `dist\黑叶萤桌宠-0.2.0-mac-arm64.zip` | 314,109,476 B |
| macOS（Intel） | `dist\黑叶萤桌宠-0.2.0-mac-x64.zip` | 319,773,758 B |

0.1.0 的旧快照已重命名为 `dist\win-unpacked-0.1.0\` 保留，作为版本间比对基线。

### 顺带补上的打包缺口

第六节列的「`build.files` 缺 `audio/`」**已修**：白名单加入 `"audio/audio_server.py"`，
并显式排除 `audio/venv`（418 MB，且 venv 绝对路径不可搬迁）与 `audio/__pycache__`。
实测包内 `resources\app\audio\audio_server.py` 存在（16,660 B）、`audio\venv` 不存在。

### 发布配置（新增机制）

源码 `config.json` 里有几处只在本机成立的绝对路径，照原样打包会让别人打开是**空白窗**，
因此在打包期间切换成发布配置：

| 字段 | 本机值 | 发布值 |
|---|---|---|
| `modelServeBase` | `D:\SteamLibrary\...\Live2DModels\Hotaru2024` | `""`（→ 回落到包内 `app\models`） |
| `modelUrl` | `/models/hotaru2024.model3.json` | `/models/Hotaru2024/hotaru2024.model3.json` |
| `acpCwd` | `D:/live2d-companion/workspace` | `""` |

实现：`tools\pack\release-config.js`（`apply` / `restore` / `show`），备份写在项目根
（不在 `build.files` 白名单内，不会被误打进包）。打完已 `restore`，本机配置未受影响。

`lineProfile` 与 `modelAssets` 的键本来就是 `Hotaru2024/hotaru2024.model3.json`，
与新的 `modelUrl` 正好对应，无需改动。

### macOS 包：electron-builder 拒绝跨平台，改为手工组装

```
⨯ Build for macOS is supported only on macOS, please see
  https://electron.build/multi-platform-build
```

**为什么不能"先解压 .app 再压 zip"**：Electron 的 `*.framework` 靠 **14 个 Unix 符号链接**
组织（`Versions/Current -> A` 等），且 `Contents/MacOS/Electron` 需要可执行位。
Windows 文件系统里这两样都不存在（建 symlink 需要特权），落盘再压缩必然丢失 → app 起不来。

**做法**（`tools\pack\build-mac.py`）：全程只在 **zip 层面**搬运 —— 读源 zip 每个条目的
`external_attr` 与 symlink 目标字符串，改名后写进目标 zip，一个字节都不落盘。

**决定性细节：`zi.create_system = 3`。**
Python 在 Windows 上新建 `ZipInfo` 时该字段为 `0`，解压端会据此**忽略 Unix 权限位**。
漏掉这一行，包在结构上看着完全正常，但装到 mac 上打不开。这是唯一一个"必须提前知道
才避得开"的坑。

其余处理：

- `Contents/Info.plist` 改写 6 个键（`CFBundleDisplayName` / `CFBundleName` /
  `CFBundleIdentifier` / 两个版本号 / `LSApplicationCategoryType`）；
  `CFBundleExecutable` 保持 `Electron`，省掉重命名 `Contents/MacOS` 下二进制的风险。
- 用 Pillow（`D:\index-tts\.venv`，11.3.0）从 `build/mac_icon.png`（1024×1024 RGBA）
  生成 7 档尺寸的 icns（1,061,629 B），替换默认的 `electron.icns`。
- `package.json` 写入裁剪版（去掉 `build` / `devDependencies` / `scripts`），与
  electron-builder 的行为一致。
- 已压缩格式（`.enc`/`.png`/`.icns` 等）用 `ZIP_STORED`，省下大量压缩时间。

### 验证结果

三份产物全部通过静态验证：

- **Windows**：`4D 5A`(MZ) 头正确；包内 `modelServeBase` 与 `acpCwd` 均为空串；
  `audio\audio_server.py`、`app\settings.js`、`app\models\...\hotaru2024.moc3.enc` 均存在；
  `app-builder-bin` / `typescript` / `7zip-bin` / `@types` 均不存在。
- **macOS（两架构各 6,326 条目）**：21 个关键文件全部就位；**符号链接 14/14**；
  `Contents/MacOS/Electron` 权限 **0o755**；Info.plist 显示名 = 黑叶萤桌宠、版本 = 0.2.0；
  顶层只有 `黑叶萤桌宠.app` 一项。判定 **`FAILS: 0 / ALL OK`**。

### 体积说明（一个被推翻的假设）

Windows 安装包 278 MB，mac 包各约 300 MB。大头是 **`app\models\Hotaru2024` 的 171.9 MB**
（`.enc` 已加密 ≈ 不可压缩，几乎 1:1 进包），其次是压缩后约 80 MB 的 Electron 运行时。

> 一度以为 `app-builder-bin`（121 MB）、`typescript`（22 MB）、`7zip-bin`（12 MB）
> 这些构建工具被打进了包，实测**并非如此** —— **electron-builder 只打包 `dependencies`**，
> 它们都在 `devDependencies` 里。所以针对它们加排除对体积没有任何实际影响
> （`build.files` 里的对应条目保留作防御）。

### 未验证

**mac 包没有在真机跑过。** 目前只证到"包结构 + Unix 元数据 + 内容完整性"这一层；
实际能否启动、macOS 下的托盘菜单表现、`globalShortcut` 热键（mac 需要用户授予辅助功能权限）、
TTS 与音律识别在缺少 Python 环境时的降级行为，都还没有实机确认。

---

## 九、本次上传（v0.2.1）：相对上一次提交 `491665d` 的全部改动

> **基线**：上一次上传 = 提交 `491665d`（2026-09-22 05:10，「独立聊天大脑体系」），已推到 `origin/main`。
> 本节只记这一批要带上去的东西；`491665d` 及更早的内容见前面第一～八节。
> 面向使用者的版本（带图）见 `更新公告.md` / `更新公告.html` / `更新公告.png`。

### 9.1 一句话总览

把两块"感知类"功能（屏幕运动追踪、音律识别）**从"能用"修到"好用"**，
外加三样新东西：**两块调试面板**、**屏幕追踪专属的额外牵动参数**、**狼族少年的人设**。
换言之，这一批里绝大多数提交都是"修"——屏幕追踪一项就在一天之内改了 **七轮**。

### 9.2 文件级改动（相对 `491665d`）

| 文件 | 变化 | 说明 |
|---|---|---|
| `main.js` | +400 / −? | 两块调试窗、托盘菜单统一延后建窗、「贴着桌宠弹」摆位、显示三保险、`second-instance` 修复、启动兜底、`display_id` 对齐、`pet:getPetWindowRect`、音律调试 IPC、`PERSONA_DEFAULT` |
| `app/js/screen-tracker.js` | +429 | 全局运动补偿、自身遮罩、人眼焦点模型（显著点+注视停留+缓动）、宠物头部为视线原点、采集健康守护、调试数据源 |
| `app/js/music-tracker.js` | +165 | 调试数据源（限流上报 + 心跳 + 只读包络 + 模型参数探针） |
| `app/js/chat-backends.js` | +139 | 情绪标签清洗（整段 + 流式）、标签铁律、`species`/`appearance` 渲染 |
| `app/js/live2d-loader.js` | +88 | 额外参数"叠加而非硬覆盖"、音律分层叠加（连续+重音）、`getMusicDebug()` |
| `app/js/app.js` | +86 | 调试面板订阅、清洗器接入、模型参数探针注入 |
| `app/data/persona.json` | +35 / −? | 活人化 + 狼族少年（`species`/`appearance`） |
| `app/js/brain-chooser.js` / `.html` | +25 / +2 | 人设表单新增「种族 / 外貌」；`collectPersona` 改为摊开旧值 |
| `app/settings.js` / `.css` | +15 / +1 | 屏幕追踪专属额外参数编辑器、「抓眼」命名、幅度量级说明 |
| `preload.js` | +6 | `getPetWindowRect` |
| `README.md` / `运行说明.md` | +1 / +2 | 三块调试面板、新页面入目录树 |
| `更新笔记.md` | — | 本节 |

合计（代码+文档）：**15 个文件，+1243 / −170**。

### 9.3 本次新增文件

| 文件 | 体量 | 说明 |
|---|---|---|
| `app/music-debug.html` | 22.2 KB / 403 行 | 音律识别调试面板：三块实时曲线 + 全量数值 + **自动诊断结论** |
| `app/screen-debug.html` | 14.1 KB / 300 行 | 屏幕追踪调试面板：捕捉画面 + 识别到的物体 + 视线箭头 |
| `tools/sim-music-debug.js` | 13.2 KB / 255 行 | 回归自检：虚拟时钟真跑 music-tracker + 新旧公式量化对比（30 条断言） |
| `tools/sim-tags.js` | 5.6 KB / 119 行 | 回归自检：情绪标签清洗（36 条断言） |

### 9.4 分项详述

#### 9.4.1 两块调试面板（新增）

沿用「屏幕追踪调试」的既有模式：渲染进程按帧上报快照 → 主进程缓存并转发 → 面板就绪时补发最近一份。
两块面板都把"到底哪一步没生效"直接呈现出来。

- **屏幕追踪调试**：实时画出捕捉画面、每个运动连通块（绿=正在追、琥珀=忽略）、桌宠自身遮罩（蓝虚线）、视线箭头。
- **音律识别调试**：① 能量 / 起音阈值 / 起音时刻竖线 / 幅度填充；② `swing`（连续相位）、`nodEnv`、`nodSway`；
  ③ **固定 ±10 刻度**下 `ParamAngleY` 与 `ParamBodyAngleZ` 的真实取值；④ 数值面板；
  ⑤ **诊断区**——按当前状态直接给结论（起音太稀疏 / 幅度被参数压成 0 / 渲染层没接管…）。

关键设计：调试快照里带一份 **`model` 字段 = 渲染层本帧真正写进模型的参数值**（由 `live2d-loader` 的
`_musicLast` 快照提供，经 app.js 注入的探针取出）。只看追踪器是看不到这一层的，
而"算出来有律动、但没写进模型"和"根本没算出来"是两种完全不同的故障。

#### 9.4.2 屏幕追踪：七轮修复

| 轮次 | 现象 | 根因 | 处理 |
|---|---|---|---|
| 1 | 点「屏幕追踪调试」报错误音、窗口不弹、托盘右键失灵，只能强杀 | 托盘菜单模态循环里同步 `new BrowserWindow` 抢不到前台焦点 | 统一延后 60ms 建窗；补上漏掉的调试 IPC 转发 |
| 2 | 设置里选屏幕 2、实际抓的是屏幕 3 | `desktopCapturer` 与 `screen.getAllDisplays()` 在 Windows 下枚举顺序不一致 | 按 `display_id` 把源对齐到 screen API 顺序 |
| 3 | 模型不跟随视线；桌宠把自己当运动物体；朝屏幕中心看 | 没调 `setScreenTrackEnabled`；无自身遮罩；视线原点写死 0.5 | 接开关；加窗口矩形遮罩；原点改为宠物头部归一化坐标 |
| 4 | 调试面板首帧收不到画面；看 FPS 视频整屏被当目标 | 采集启动可靠性；无全局运动补偿 | 行/列投影 SAD 估计整屏平移并抵消 + 铺满门限兜底 |
| 5 | 首帧仍黑屏；追加"额外牵动参数(抓眼)" | 采集启动不可靠（与"跨进程序列化丢值"无关）；该功能其实已存在，只是会硬覆盖基础管线 | 移出屏幕但保持渲染；额外参数改为**叠加** |
| 6 | 必须手动关掉再开一次才采到画面 | 首次采集偶得坏源 + 视频 `display:none` 让解码停摆 | 常驻「采集健康守护」：自动重试、卡死自愈、失败不放弃 |
| 7 | 屏幕追踪没有自己的额外参数 | 追踪器只读 `gaze.extra` 兜底 | 屏幕追踪页新增专属编辑器（复用既有 `pet:setScreenTrackParams` 通道） |

#### 9.4.3 音律识别：从"偶尔抽动"到"连贯晃动"

- 打包快照里 `_musicTick` 是靠**连续相位正弦**驱动的（`bob=-swing*5`、`sway=swing*4`）。
  后来为治"快歌下自由正弦被读成抽搐"，改成只读每拍一次性的 `nodEnv`/`nodSway`，
  结果**连续项在渲染层成了孤儿**（追踪器仍在算 `getSwing()`，没人读），动作完全由起音决定 → 起音一稀疏就整段静止。
- 改为**分层叠加**：连续项（相位速度 = A 方案 `aSpeed` / B 方案锁定 BPM，受 `maxTempo` 折半限制）+ 每拍重音项。
  身体以连续项为主体（×4），头部以重音为主体（×5）+ 小幅连续项（×2）。
- 量化（`tools/sim-music-debug.js`，起音间隔 2.5 s）：旧写法 **94% 的帧身体完全静止**（RMS 0.57），新写法仅 **4%**（RMS 2.31）。
- 另：B 方案关闭时 A 方案仍会推进相位，所以"不锁 BPM"也有连续晃动；A/B 全关时 `level` 收敛到 0，不会僵在某个角度。

#### 9.4.4 启动级故障：关掉再打开就打不开了

三个故障叠加，逐个定案（全部有日志/探针实证）：

1. **托盘菜单回调里同步建窗**（与屏幕追踪第 1 轮同源）→ 统一 `deferMenuAction`，这次把「设置」「切换聊天大脑」也补上。
2. **「下次不再询问」复选框恒为未勾选、也不按配置初始化** → 每次确认都把 `chat.askEveryStart` 悄悄写回 `true`，
   于是每次启动都先拦在"选大脑"这一步，主窗口根本没被创建；配置里那份被写坏的值已一并复原。
3. **`second-instance` 在无主窗口时什么都不做**（旧实现只 `mainWin.show()`，此刻 `mainWin` 还是 null）→
   改为把任意存活窗口提到前台，一个都没有才 `bootPet()`（并加"退出流程中不建窗"守卫）。
4. 顺带：**「设置窗弹不出来」其实是摆位问题**——主屏 3440×1440 超宽屏、桌宠停在最右侧，
   而 Electron 默认居中到主显示器（x≈1330，用户视野左半边）。改为「贴着桌宠弹」（优先左、再右、最后居中，夹回工作区）。

#### 9.4.5 人设：狼族少年 + 活人化

- `persona.json` v2 新增 `species`（狼族少年）与 `appearance`（狼耳 / 狼尾及其情绪表现），
  `personality` / `boundaries` / `speech` 相应补充；提示词构建会把这两人字段渲染进去。
- **人设表单的字段漂移 bug**：`collectPersona()` 原本从零拼新对象，只包含表单有的输入框 ——
  于是给 JSON 加字段却没加输入框时，界面显示正常、读取正常，**用户点一次「确定」就把它抹掉且不报错**。
  已改为 `Object.assign({}, old, {...})`，并补上「种族 / 外貌」两个控件。

#### 9.4.6 情绪标签不再漏进气泡

- 旧清洗只认「本次流片段以 `[` 开头」，而 Ollama 常送来 `" ["`（空格+方括号）或整块 `[sleepy]`，两个条件都不满足 → 原样显示。
- 改为：整段清洗 `stripEmotionTags` + 流式 `createTagStripper`（半个标签跨片段时把待定尾巴与本次片段**拼起来再判断**），
  支持全角 `【】` 与中文标注；`a[0]`、`[1,2]`、`a[i]`、`[带空格]`、4 字以上中文括号一律保留。
- 提示词加【标签铁律】：只允许在最开头写一枚，正文里禁止任何方括号标注与括号动作。

### 9.5 验证（都是实跑，非推断）

- **回归自检**：`tools/sim-tags.js` 36 条断言全过；`tools/sim-music-debug.js` 30 条断言全过
  （后者用虚拟时钟 + 桩 AudioContext **真跑** `music-tracker.js`，并做新旧公式量化对比）。
- **端到端 Electron 探针**（用完即删）：
  - 音律识别调试：钩住托盘实例 → 触发右键捕获菜单模板 → 点「调试 → 音律识别调试」→
    窗口创建且 `visible`（紧贴桌宠）、4.5 s 收到 **55 帧**、`model={angleY,bodyZ}` 有值、诊断区准确指出"身体摆幅系数只有 0.10"、渲染进程零报错。
  - 人设表单：真 `main.js` + 真 `preload.js` 打开 `brain-chooser.html`，读回 `species=狼族少年`，
    **点一次「确定」保存后逐字段比对无丢失**；用户配置（`chat` / `screenTrack` / `gaze`）未被改动。
  - 启动流程：`askEveryStart=false` 时桌宠自动可见；点「设置」→ 设置窗创建且可见、坐标紧贴桌宠；
    第二实例启动 → 日志出现"把已有窗口提到前台"。
- 全部改动文件语法检查通过（含两个调试页的内联脚本）。

### 9.6 仍遗留

- 实际观感（屏幕追踪的"看焦点"手感、音律识别的晃动幅度）仍建议**客户端实测**——无 GUI 环境只能验证到逻辑层。
- `LICENSE` 仍未附加（README 现写"尚未附加开源许可证"）。
- 「启动程序」本地命令表（`docs/方案-桌宠独立大脑.md` 里提过）未做。
- 空闲台词档（`app/data/idle-lines.json` / `click-lines.json`）仍是旧的敬语口吻，与活人化人设不一致；用户未要求，暂未改。

---

*本笔记据打包快照比对（161 → 281 文件）、文件修改时间扫描（47 个命中）、以及各次工作日志交叉核对写成。*
